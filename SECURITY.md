# 安全策略

## 支持范围

world-sim 是**个人研究性质的项目，不提供安全支持 SLA**。以下类型的问题仍然欢迎报告，维护者会尽力处理：

- 凭据泄露 / 硬编码密钥 / 意外的信息暴露
- 可被远程利用的指令执行路径或注入点
- 依赖组件的已知严重漏洞（CVE），尤其是会被默认配置启用的

不视为安全问题的（请走普通 issue）：

- 数据源失效、指标口径争议、推演结果不符合预期
- 在已明确标注「仅供研究」前提下的输出准确性问题

---

## 如何报告

**请不要开公开 issue**，也不要在公开的讨论区贴细节 —— 那会在修复之前就把问题暴露出去。

请通过以下渠道私有提交：

1. GitHub 仓库页 → **Security** → **Report a vulnerability**（私有安全公告，推荐）
2. 若不便使用上述渠道，可在 issue 中只留一句「有一个安全问题需要私下沟通」，等维护者联系

报告时请尽量包含：

- 问题类型与影响面（能读到什么 / 能执行什么 / 需要什么前置条件）
- 复现步骤（命令、配置、环境版本）
- 涉及的文件路径或提交号
- 如果已有修复思路，一并提出会更快

**预期响应**：维护者会在能力范围内尽快确认；若判断为有效问题，会先出修复再公开说明。请勿在收到回复前公开披露。

---

## 部署者须知

自行部署本项目时，请特别注意以下几点：

### 1. `.env` 绝不进仓库

所有密钥、口令、主题名都通过 `.env` 注入。`.env` 已被 `.gitignore` 覆盖，**请勿提交真值，也不要把真值写进 `.env.example`**。

公开部署前，建议自行检查一遍仓库里有没有意外带入的凭据：

```bash
git grep -nE '(sk-|ghp_|xox[baprs]-|AKIA)[A-Za-z0-9]{10,}' -- .
```

### 2. 通知 / 指令通道建议自建

本系统使用 [ntfy](https://ntfy.sh/) 做推送，并且包含一个**远程指令监听**能力：监听器轮询一个指令主题，收到消息后按预设指令执行，**执行前会校验一个共享密钥**。

这意味着：

- 如果指令主题托管在**公共 ntfy 服务**上，那么**任何知道主题名的人都能订阅并看到你的指令内容**；
- 此时「内部低风险」的前提不再成立 —— 保护你的只剩那个共享密钥，**它必须足够强**。

**建议**：生产部署请把 `NTFY_BASE_URL` 指向你自建的 ntfy 实例，并为 `NTFY_CMD_SECRET` 设置高强度随机值。

⚠️ **部署前请自行核对**：确认你配置的 `NTFY_BASE_URL` 在**指令监听路径上是否真的生效**（部分实现的监听地址可能是独立写死的，不会读取该变量）。若无法确认，请视为指令通道暴露在公网，并使用强密钥。

### 3. 默认值可能指向原部署环境

本项目原部署在内网环境，部分默认值（如代理地址、数据库主机）带有私有网络特征。在你自己的环境里需要逐一覆盖，或留空走直连。

### 4. 输出不构成建议

系统输出由自动化流程生成，可能存在错误或过时信息，**请勿作为投资、交易或任何决策的依据**。

---

## 关于仓库内的已知大文件

`macro-scan/wheels/` 下包含一个约 46 MB 的中文 NLP 模型 wheel（spaCy `zh_core_web_sm`），用于离线构建时的依赖安装（Dockerfile 通过 `--find-links` 引用），**非恶意文件**。若你不需要离线构建，可以自行改用 PyPI 安装并移除该目录。
