# 世界推演系统 · 待办事项

> 最后更新：2026-07-08 v3.5.31-v3.5.32 修复

---

## ✅ 本次已完成（2026-07-08 v3.5.31~v3.5.32）

| # | 内容 |
|---|------|
| v3.5.31 | `situation_detector.py`：`ALERT_KEYWORDS` 改为从 `alert_config` 直接导入（原来间接从 `scan_weak_signals` 导入，有 silent fallback 风险），加 `assert` fail-fast |
| v3.5.31 | `news_exporter.py`：`CATEGORY_MAP` 加注释标注「文化贸易摩擦」「科技竞争」「自然灾害」三类为已知 backlog（macro-sim 侧暂无对应 handler）|
| v3.5.31 | `hypothesis_engine.py`：`_compile_wiki_entry()` 中 `content_hash` 重复检查移入 `_wiki_lock` 内，修复 TOCTOU 竞态 |
| v3.5.31 | `geo_risk_vector.py`：`_normalize_gpr` 退化 fallback 加 `logger.warning`；`append_grv_history` 加日期去重（防多次补跑产生同天重复记录）|
| v3.5.32 | `hypothesis_engine.py`：新增 `run_hypothesis_simple(hypothesis_text, depth)` 子进程调用入口，自行从磁盘读 `grv_latest.json` 构造 indicators，懒导入 rag_engine/hybrid_llm，委托给 `run_hypothesis()` |
| v3.5.32 P1修复 | `signal_synthesizer.py:487`：`run_hypothesis(hypothesis)` → `run_hypothesis_simple(hypothesis)`（修复 Live 模式 P1 bug：原只传1个参数被 except 吞掉，llm_ok 永远为0，冷却机制从未触发，假设推演从未执行）|
| v3.5.32 P1修复 | `grv_threshold.py:186`：同上，`run_hypothesis(scenario)` → `run_hypothesis_simple(scenario)` |
| v3.5.32 | `regime_detector.py`：`NBER_RECESSIONS` 后加陈旧检测，超3年未更新时发 `UserWarning` |

---

## ✅ 本次已完成（2026-06-30 v3.5.25~v3.5.26）

| # | 内容 |
|---|------|
| v3.5.25 | `build_rag_index.py` / `rag_engine.py`：移除 Ollama 依赖，embedding 统一走硅基流动 BAAI/bge-m3 |
| RAG重建 | 2844 → **4156 块**（558个.md，覆盖 21_专题报告/ 等新增目录）|
| v3.5.26 | 删除废弃文档：`世界推演系统_AI说明文档.md` / `docs/decisions` / `docs/待办事项.html` / `知识库扩展方案_v1+v2.md` |
| v3.5.26 | 新增 `docs/知识库扩展方案.md`（v1+v2合并版，含完整实施状态）|
| 文档同步 | `世界推演系统_人类说明文档.md` + `.html` 同步更新至 V3.5.26 |
| 文档审查 | AGENTS.md / INDEX.md / FILE_MANIFEST.md / 目录结构文档 全部修正过时引用 |

---

## ✅ 本次已完成（2026-06-27 worktree-macro-scan-refactor，v3.5.3~v3.5.6）

| # | 内容 |
|---|------|
| P1-Bug | `hypothesis_engine.py` TYPE_FIELD_MAP/TYPE_KEYWORDS 提升模块级常量，修复 TRADE/CRISIS 双重赋值 |
| P1-Bug | `rag_engine.py` SKIP_DIRS 加入 `07_分析报告`，阻断 RAG 自引用循环 |
| P2-Bug | `situation_tracker.py` watching 状态0信号时降级为 calm |
| P2 | `alert_config.py` 新建：提取 ALERT_KEYWORDS/\_WATCH_COUNTRIES/\_ACTOR_* |
| P2 | `optim_config.py` + `data_fetcher.py`：KEY_INDICATORS 迁至 optim_config，消除重复定义 |
| P3 | `synthesis_rules.yaml` R01-R05 加 min_articles_per_category: 3 降低误报 |
| P3 | `news_db.py` 新增 prune_old_articles(90天) + scheduler 月度清理 |
| P3 | `hypothesis_config.py` 新建：外置 DIM_MAP |
| P3 | `rag_engine.py` 新增 rag_query() 统一入口（向量+TF-IDF fallback） |
| P4 | `fetch_rss_news.py` RSS_ROUTES 追加 FT/BBC/Reuters/日経 EU/JP 路由 |
| P4 | `optim_config.py` KEY_INDICATORS 追加7条 EU/JP FRED 序列（32条） |
| P4 | `grv_threshold.py` cooldown 用 fcntl.flock 原子化 |

## ⏳ 时间门控

| 时间 | 任务 |
|------|------|
| 2026-07-10 | signal_synthesizer 切 Live（STAGING_MODE=True→False） |
| 2026-07-10+ | 评估 R09/R10 是否激活（social_stress/cultural_friction 积累3周）|

---

## ✅ 本次已完成（2026-06-12 Bug修复 + Hermes摸底）

| # | 内容 |
|---|------|
| SQL严重Bug修复 | `situation_tracker.py` `_query_recent_articles()` SQL OR逻辑错误 → 所有事件虚假escalating；已修复为正确 LIKE 过滤 + 重置6个事件状态 |
| AI文档镜像修正 | `世界推演系统_AI说明文档.md` L24 macro-scan:v6 → v7 |
| Hermes摸底确认 | disaster 05:25未触发/GRV null/scheduler 3天重启均为预期行为，明日05:25起正常 |

---

## ✅ 本次已完成（2026-06-12 V3.5）

| # | 内容 |
|---|------|
| GRV扩展 | `geo_risk_vector.py`：`grv_latest.json` 新增 `climate_risk` + `disaster_risk` 两个维度 |
| 框架文档 | `科技竞争与出口管制框架.md` + `稀土与战略矿产供应链.md`（各含反面条件+历史锚点）|
| 传导路径 | propagation_paths.yaml 18→**20条**：新兴市场政治货币危机（巴基斯坦/土耳其验证）+ 土耳其里拉/地缘 |
| 周报 | `weekly_synthesis.py`：每周五20:00；1900 weekly 可触发；含持续信号/GRV变化/情境动态/日历 |
| situation触发推演 | escalating 时自动 L1 轻量推演（非阻断）|
| Web UI GRV时序图 | 📈 新标签页；Canvas 原生绘制7天折线图（4维）；/grv-history API |
| verify自动调度 | scheduler.py：每月1日09:15自动 `--commit --update-weights` |
| ntfy新指令 | `1900 weekly`（本周综合回顾）|
| RAG重建 | 2834→**2844块** |

---

## ✅ 本次已完成（2026-06-11 自然灾害完整链路 V3.4）

| # | 内容 |
|---|------|
| 框架文档 | `自然灾害系统性风险.md`：灾害分级/日本2011复盘/6条反面条件/传导时序量化 |
| 传导路径 | propagation_paths.yaml 新增2条（18条+5原子）：`natural_disaster_supply_chain`(cal=0.58) + `earthquake_nuclear_cascade`(cal=0.65) |
| 实时数据源 | `fetch_disaster_signals.py`：USGS M≥2.5地震/24h；关键供应链区位加成；核电密集区标注；每日05:25 |
| 系统集成 | scheduler.py 追加05:25 disaster任务；daily_narrative 注入灾害信号；web_server /status 加灾害风险字段 |
| RAG重建 | 2800→**2834块**（自然灾害框架文档34块）|
| 文档更新 | AI说明文档/人类说明文档→V3.4；HTML时间线加05:25节点；域覆盖图自然灾害✅ |

---

## ✅ 本次已完成（2026-06-11 全面覆盖扩展 V3.3）

| # | 内容 |
|---|------|
| 地缘覆盖 | 别名新增：巴基斯坦/土耳其/尼日利亚/埃及；GEO keywords 补土耳其/非洲/太空 |
| 战略矿产 | 别名新增：稀土/锂矿；TRADE keywords 补稀土/锂/钴/镍/矿产禁令；ALERT_KEYWORDS 新增"战略矿产"类别 |
| 科技竞争 | 别名新增：AI监管/量子/太空；CRISIS keywords 扩展；ALERT_KEYWORDS 新增"科技竞争"类别 |
| 自然灾害 | 别名新增：地震/海啸/火山；CRISIS keywords 扩展；ALERT_KEYWORDS 新增"自然灾害"类别 |
| 反馈环检测 | `_detect_feedback_loops()`；[FEEDBACK] 警告节注入推演提示词（6种正反馈模式）|
| 历史案例 35→41 条 | 印度洋海啸/日本地震+核/巴基斯坦政治+洪灾/土耳其大地震/EU AI法案（渐进式反例）|
| 文档更新 | AI说明文档/人类说明文档 → V3.3（别名48个速查表）|

## ⏳ 盲区 P2/P3 剩余待处理

| 优先级 | 盲区 | 说明 |
|:---|:---|:---|
| P2 | 置信度衰减机制 | 历史锚点的市场学习效应未建模（架构级）|
| P2 | 多路径交叉干扰 | 多情景同时活跃时的协同分析 |
| P3 | 非洲/南亚传导路径 | 别名已加，但无专属传导路径（目前走 UNKNOWN 兜底）|
| P3 | L4 极端尾部场景建模 | 核冲突/全球疫病走"情景型"推演 |
| P3 | 假阳性率回测框架 | 历史重大事件回测当前规则引擎信噪比 |

---

## ✅ 本次已完成（2026-06-11 中东/航道覆盖修复 + 盲区 P1）

| # | 内容 |
|---|------|
| HQ-001 | `hypothesis_templates.yaml`：别名新增10个（伊朗/中东/以色列/沙特/印度/霍尔木兹/马六甲/苏伊士/美国/中国）；GEO keywords 大幅扩展（含航道/中东/南亚）|
| HQ-001 | `hypothesis_engine.py`：type_field_map/type_keywords/geo_cats 全部同步扩展 |
| HQ-001 | `scan_weak_signals.py` ALERT_KEYWORDS["能源政治"] 补充 Hormuz/Houthi/Red Sea/Suez/Malacca 等 12 个关键词 |
| HQ-001 | `历史情景_量化指标.csv`：32→35 条（新增沙特阿布盖格袭击/胡塞红海/苏伊士搁浅）|

## ⏳ 系统性盲区（HQ-20260611-002，P2/P3 待处理）

| 优先级 | 盲区 | 说明 |
|:---|:---|:---|
| P2 | 印度/巴基斯坦深度覆盖 | 仅加了别名，缺传导路径和历史案例 |
| P2 | 土耳其地缘覆盖 | 地区性不稳支点，目前零覆盖 |
| P2 | 自然灾害域（地震/火山/海啸）| GDELT 也无此类 CAMEO 码，需新数据源 |
| P2 | 战略矿产/稀土覆盖 | 中国稀土管制、锂矿集中度等 |
| P2 | 科技竞争域（AI/量子/太空）| 目前通过 TRADE 散点覆盖，无专属域 |
| P2 | 传导路径反馈环建模 | 架构层：A→B→C→A 正反馈/负反馈 |
| P2 | 置信度衰减机制 | 历史锚点的市场学习效应未建模 |
| P3 | 非洲（尼日利亚/埃及）覆盖 | 社会/气候/政治叠加，目前零覆盖 |
| P3 | L4 极端尾部场景建模 | 核冲突/全球疫病，需"情景型"而非"类比型"推演 |
| P3 | 假阳性率回测框架 | 把当前规则引擎回测到历史重大事件 |

---

## ✅ 本次已完成（2026-06-11 知识库扩展方案 + 代码审查修复）

| # | 内容 |
|---|------|
| P0-1 | `hypothesis_templates.yaml`：新增 CLIMATE/CULTURAL 两个域（别名9个 + categories + synergy_rules 3条）|
| P0-2 | `hypothesis_engine.py`：5处联动修改（type_field_map/type_keywords/geo_cats/dim_map/复合路径匹配）|
| P0-3 | `历史情景_量化指标.csv`：从23条扩展到32条（+9条：含1条反例2016超强厄尔尼诺无危机）|
| P0-4 | `fetch_fred_history.py`：追加小麦/玉米/美国青年失业率 3条序列（共36条）|
| P1-1 | `气候风险传导框架.md`（新建）：ONI分级×接口/主产区矩阵/国家脆弱性快查/2016反例 |
| P1-2 | `社会脆弱性评估框架.md`（新建）：四维评分模型/斯里兰卡模型/GDELT接入 |
| P1-3 | `政治周期与资产传导.md`（新建）：EPU/政治日历窗口/两类国家差异 |
| P1-4 | `文化摩擦与软实力冲突.md`（新建）：L1/L2/L3三级/行业分类替代概率 |
| P1-5 | `商品地缘风险速查表.md`（新建）：12大战略商品地缘矩阵 |
| P1-6 | `新兴市场脆弱性速查表.md`（新建）：20国四维评分/分层自动化 |
| P1-7 | `propagation_paths.yaml`：扩展至16条+5条原子路径；新增 applicable_scenarios 字段 |
| P2-1 | `situation_detector.py`（新建）：每日06:30，TF-IDF+LLM，9项安全设计 |
| P2-2 | `ntfy_listener.py`：新增 confirm_situation / dismiss_situation 指令（通过 update_situation 接口）|
| P2-3 | `daily_narrative.py`：接入气候信号 + 区分已确认/待确认情况 |
| P2-4 | `synthesis_rules.yaml`：追加 R11（气候×社会）/ R12（多域系统性，修复categories错误）|
| P2-5 | `新兴市场脆弱性速查表.md`（已含在P1-6）|
| P2-6 | `scheduler.py`：追加 situation_detect 06:30 任务 |
| RAG | 索引重建至 **2800 块**（含6篇新框架文档）|
| 代码审查F1 | `synthesis_rules.yaml` R12 categories：`"地缘升级"` → `"流动性危机"` |
| 代码审查F2 | `situation_tracker.py`：新增 `update_situation()` 原子性接口 |
| 代码审查F3 | `ntfy_listener.py`：confirm/dismiss 改用 `update_situation` 接口，修复并发写入风险 |
| 代码审查F4 | `situation_detector.py`：`needs_review` 加默认值 False |
| 代码审查F5 | `hypothesis_engine.py`：`dim_map` grv_val 加 `isinstance` 类型检查 |
| 文档更新 | AI说明文档 → V3.1；人类说明文档 → V3.1（清理旧版残留）|
| 方案文档 | `docs/知识库扩展方案_v1.md` + `docs/知识库扩展方案_v2.md` 已写入 |

## ⏳ 待触发（时间门控）

| # | 条件 | 内容 |
|---|------|------|
| C线切Live | `signal_episodes` 数据年龄 ≥ 30天（约2026-07-10）| `signal_synthesizer.py` 第18行 `STAGING_MODE=False` |
| R07开启 | `religious_conflict` 维度有非零分值（约2026-07-01）| `synthesis_rules.yaml` R07 `enabled: true` |
| R09开启 | `social_stress` 维度积累3周非零数据（约2026-07-01）| `synthesis_rules.yaml` R09 `enabled: true` |
| R10开启 | `cultural_friction` 维度积累3周非零数据（约2026-07-01）| `synthesis_rules.yaml` R10 `enabled: true` |
| R11/R12开启 | 气候/多域信号积累基线（约2026-08-01）| `synthesis_rules.yaml` R11/R12 `enabled: true` |
| GDELT scale 校准 | 运行3个月后（约2026-09-10）| 用 `gdelt_history.jsonl` 校准 scale 参数 |
| situation_detector 评估 | 运行2周后（约2026-07-15）| 评估话题检测质量，调整 `_FREQ_RATIO_THRESHOLD`（当前2.0）|
| jieba 安装 | situation_detector 首次运行前 | `docker exec macro-scan pip install jieba`（可选，无则降级regex）|
| N2 | N1上线≥180天（2026-11-19）| 新闻库第二阶段（反向查询）|
| N3 | N1上线≥1年（2027-05-23）| 新闻库第三阶段（信号校验）|
| V3.1 备份 | 近期 | `S:\macro-scan\备份\世界推演系统_V3.1_2026-06-11.zip` |

---

## ✅ 本次已完成（2026-06-10 Phase 1~3 完善方案）

| # | 内容 |
|---|------|
| Phase 1A | `ntfy_listener.py`：新增 `cmd_ask()`（自由问答，Q&A模式，约30秒）；`handle()` 路由追加 ask/situations/narrative |
| Phase 1B | `daily_narrative.py`（新建）：07:00 自动推送今日世界3件事；`scheduler.py` 追加调度 |
| Phase 1C | `situation_tracker.py`（新建）：追踪6个预设事件状态；`scan_weak_signals.py` run_scan() 末尾接入 |
| Phase 1D | `signal_synthesizer.py`：新增 `_check_asset_correlation_shift()`（R08，4对跨资产相关性突变检测，Δcorr≥0.4）|
| Phase 1E | `fetch_fred_history.py` SERIES 追加4个：DEXCHUS/PCOPPUSDM/WDTGAL/ICSA |
| Phase 2A | `fetch_climate_signals.py`（新建）：NOAA ONI 厄尔尼诺 + FIRMS 火点；每月1日09:10 |
| Phase 2B | `scan_weak_signals.py`：GDELT `social_stress`（Goldstein均值）维度 + R09规则（初期disabled）|
| Phase 2C | `知识库/political_calendar.yaml`（新建）：15条重要政治/宏观事件（2026~2027）|
| Phase 2D | `scan_weak_signals.py`：GDELT `cultural_friction`（EDU/MED Actor摩擦）维度；ALERT_KEYWORDS 追加"文化贸易摩擦"；R10规则（初期disabled）|
| Phase 3A | `web_server.py`（新建）：FastAPI Web UI，端口8899；三面板（问答/事件追踪/系统状态）；`docker-compose.yml` 已加端口映射 |
| Phase 3B | `hypothesis_engine.py`：`run_hypothesis()` Step 7 新增 depth=deep 分支（三轮链式推理）；`ntfy_listener.py` `cmd_hypothesis()` 已正确传递 `--depth` 参数 |
| Phase 3C | `verify_hypothesis.py`：新增 `update_path_weights()` 贝叶斯更新；`--update-weights` 参数 |
| 方案文档 | `IMPROVEMENT_PLAN.md`（新建）：Phase 1~3 完整规划 |
| 文档更新 | `世界推演系统_AI说明文档.md` → V3.0；`世界推演系统_人类说明文档.md` → V3.0 |
| 容器重建 | `docker compose up -d` 已执行，端口8899生效；Web UI 可访问 `http://192.168.31.108:8899` |

## ⏳ 待触发（时间门控）

| # | 条件 | 内容 |
|---|------|------|
| C线切Live | `signal_episodes` 数据年龄 ≥ 30天（约2026-07-10）| `signal_synthesizer.py` 第18行 `STAGING_MODE=False` |
| R07开启 | `religious_conflict` 维度有非零分值（约2周）| `synthesis_rules.yaml` R07 `enabled: true` |
| R09开启 | `social_stress` 维度积累3周非零数据 | `synthesis_rules.yaml` R09 `enabled: true` |
| R10开启 | `cultural_friction` 维度积累3周非零数据 | `synthesis_rules.yaml` R10 `enabled: true` |
| GDELT scale 校准 | 运行 3 个月后（约2026-09-10）| 用 `gdelt_history.jsonl` 校准 religious_conflict/regime_change scale 参数 |
| fastapi 安装 | 首次访问 Web UI 前（如未自动安装）| `docker exec macro-scan-macro-scan-1 pip install fastapi uvicorn pyyaml` |
| V3.0 备份 | — | `S:\macro-scan\备份\世界推演系统_V3.0_2026-06-10.zip` |

---

## ✅ 本次已完成（2026-06-10 B线+C线+社会信号扩展）

| # | 内容 |
|---|------|
| B线 | `grv_threshold.py`（新建）+ `geo_risk_vector.py` 接入：台海≥68 OR 单日涨幅≥6 自动触发推演，GRV_DRY_RUN=1安全验收 |
| C线 | `signal_synthesizer.py`（新建，STAGING_MODE=True）+ `synthesis_rules.yaml`（R01-R07，R07初期关闭）|
| 推送分级 | `ntfy_listener.py`：push_text_with_priority() + synthesize/silence 两条新指令 |
| synthesis_log | `news_db.py` 追加 synthesis_log 表 DDL |
| GDELT扩展 | `scan_weak_signals.py`：新增 religious_conflict/regime_change 两维度；_WATCH_COUNTRIES 扩至17国；ALERT_KEYWORDS 追加3类 |
| 传导路径 | `propagation_paths.yaml`：新增4条社会类路径（总计14条）|
| 路由扩展 | `hypothesis_engine.py`：SOCIAL/POLITICAL/RELIGIOUS 路由+置信度天花板🟡 |
| 验收 | 7/7 语法通过；synthesis_log建表✅；B线DRY_RUN触发✅；关键词命中✅；路径写入✅ |

## ⏳ 待触发（时间门控）

| # | 条件 | 内容 |
|---|------|------|
| C线切Live | `signal_episodes` 数据年龄 ≥ 30天 | `signal_synthesizer.py` 第18行 `STAGING_MODE=False` |
| R07开启 | `religious_conflict` 维度有非零分值（约2周）| `synthesis_rules.yaml` R07 `enabled: true` |
| GDELT scale 校准 | 运行 3 个月后 | 用 `gdelt_history.jsonl` 校准 religious_conflict/regime_change 的 scale 参数 |

---

## ✅ 本次已完成（2026-06-09 CF-15 + CF-16 + CUCI-1~5）

| # | 内容 |
|---|------|
| CF-15 | `run_macro_analysis.py` 函数签名 `reasoning="local"` → `"auto"`；macro-scan 容器 `up -d --force-recreate` 注入 OPENAI_COMPAT_* + USE_EXTERNAL_LLM；验证：MiMo status=200，3064字符，81.4s ✅ |
| CF-16 | Crucix OOM Kill 修复：`mem_limit` 700m→1024m；OpenSky 改串行 bbox；Telegram 推送 300KB cap；`--max-old-space-size` 350→512 |
| CUCI-1 | `get_current_snapshot()` 末尾写入 `snapshot["_crucix"]`（gscpi/nuke/sdr/air/vix）；修复 5 处旧"只过滤不写入"的占位（P0-1 完成）|
| CUCI-2 | `regime_detector.py`：`detect_regime()` 末尾计算 `gscpi_warn`；`get_regime_info()` 新增 `gscpi_warn`/`gscpi_value` 返回字段 |
| CUCI-3 | `generate_report()` 签名加 `crucix_context: str=""`；主流程构建 crucix_context（GSCPI警告+核辐射异常+4区域航空）；三处 `generate_report()` 调用追加参数；验证：quick日报注入5条信号 ✅ |
| CUCI-4 | crucix `.env` 填入 ACLED/EIA/FIRMS key；修复末行无换行导致 NO_PROXY 污染 |
| CUCI-5 | `firms.mjs` 原生 fetch → `safeFetch`（走代理）；docker-compose.yml environment 段显式加 EIA_API_KEY/FIRMS_MAP_KEY；验证：FIRMS 6区域全通 ✅，EIA oil/gas 正常 ✅ |
| P0-1 | **已确认完成**（CUCI-1 覆盖）：_crucix 键现已在 `get_current_snapshot()` 正确写入，5处旧占位均已生效 |
| GRV | 当前值（2026-06-09 06:10）：台海=60.2 / 中美=53.5 / 俄欧=29.7 / 中东能源=42.0 |
| ACLED | ❌ Gmail 账号注册不授权数据，已放弃；日报最小注记方案（P1 可选，~15行，零独立模块）未实施 |
| 文档 | AI说明文档 → V2.3（含 MiMo 降级链、ACLED 不可用、CUCI-3/5 说明） |

---

## ✅ 本次已完成（2026-06-04 CF-14 MiMo 切换 + 调度修复）

| # | 内容 |
|---|------|
| CF-14 | `docker-compose.yml` 新增 OPENAI_COMPAT_URL/KEY/MODEL/USE_EXTERNAL_LLM（MiMo）；`hybrid_llm.py` `_CALL_LOCAL_MAX_RETRIES` 2→1；`scheduler.py` China 日报 20:05→20:15（避免锁冲突）|
| 降级链 | MiMo（主力）→ Claude API（无key跳过）→ SiliconFlow Qwen3.5-27B → 纯数据报告 |

---

## ✅ 本次已完成（2026-06-04 CF-13 中国报告 max_tokens 不足截断）

| # | 内容 |
|---|------|
| CF-13 | `run_macro_analysis.py:2244` `call_ollama()` 的 `max_tokens` 从 4096 提升至 6144（+50%） |
| 根因 | Qwen3.5-27B 在 max_tokens=4096 下，中国部分"风险1+2+3" 后触顶被服务端截断，CF-12 重试逻辑不触发（截断是有效响应而非空响应） |
| 验证 | 12:39 重跑复现 6104字节，中国部分仅"风险1+2+3"后截断；修复后待 20:00/20:05 调度报告或手动触发验证 |
| 文档 | 待补：AI说明文档/人类说明文档 V2.3 需注明 max_tokens 变更 |
| CHANGELOG | `TuiYan_CHANGELOG.md` 新增 CF-13 条目 |

## ✅ 本次已完成（2026-06-03 CF-12 SiliconFlow 空响应自动重试）

| # | 内容 |
|---|------|
| CF-12 | `hybrid_llm.py` 重构 `call_local()`：新增 `_do_siliconflow_request()` 单次请求函数 + `_RetryableError` 异常类；空响应/429/503/超时自动重试最多2次（间隔5s/10s） |
| 根因 | SiliconFlow Qwen3.5-27B 间歇性返回 HTTP 200 但 `choices[0].message.content=""`，原代码直接 raise ValueError 无重试，触发降级为纯数据报告 |
| 验证 | ntfy 手动触发 `both quick` → 新报告7611字节（降级版3725字节），包含"滞胀初现"/"二次通胀风险"等完整LLM叙事 |
| 文档 | AI说明文档/人类说明文档/HTML版 更新至 V2.2 |
| CHANGELOG | `TuiYan_CHANGELOG.md` 新增 CF-12 条目 |

## ✅ 本次已完成（2026-06-02 Bug #3 单国模式修复）

| # | 内容 |
|---|------|
| Bug #3 | `run_macro_analysis.py` 单国模式（`country=us`/`china`）访问未定义的 `us_report`/`china_report` → `UnboundLocalError`；添加 `if country == "both"` 分支修复 |
| 文档 | AI说明文档/人类说明文档 更新至 V2.1 |

## ✅ 本次已完成（2026-06-01 CF-10 + CF-11）

| # | 内容 |
|---|------|
| CF-10 | `call_ollama()` 统一转发给 `hybrid_llm.reason()`，修复定时报告仍走死掉的 Ollama 问题 |
| CF-11 | `save_report()` 新增 `is_fallback: bool` 参数；both模式仅双方均降级才标 `[降级模式]` banner |
| 文档 | AI说明文档/人类说明文档/HTML版 更新至 V2.1 |

---

## ✅ 本次已完成（2026-05-30 地缘推演增强方案 P0+M1+M2）

| # | 内容 |
|---|------|
| P0-1 | `历史情景_量化指标.csv` 扩充至23条（+10条地缘案例），新增 `crisis_category`/`taiwan_strait_relevance`/`vix_peak` 3列 |
| P0-2 | `calibrate_mc.py` 补充3字段读取 + `_severity_bucket()` 加"区域地缘"分支 |
| P0-3 | `hypothesis_engine.py` 类比评分加 crisis_category 命中+3分 + TAIWAN子类型 tw_rel×2 权重 |
| P0-4 | `hypothesis_engine.py` build_hypothesis_prompt() 注入 GDELT 地缘信号（读缓存，不发网络） |
| M1-1 | `fetch_gpr.py`（新建）：GPR官网XLS下载，7系列，496行/系列，每日05:40运行 |
| M1-2 | `geo_risk_vector.py`（新建）：GDELT+GPR→5维GRV向量，每日06:10运行，当前台海=45.6/中美=53.0 |
| M1-3 | `propagation_paths.yaml`（新建）：10条结构化传导路径，[PATHS]节注入推演提示词 |
| M2-1 | `compute_confidence()`（新建函数）：4维置信度调和平均，台海L2实测0.66🟡 |
| M2-2 | `_compile_wiki_entry()` 升v2：新增 grv_at_inference/confidence_breakdown/key_assumptions 字段 |
| M2-3 | `build_hypothesis_prompt()` 重构：进度条式置信度分解 + 强制要求LLM引用PATHS编号 |
| M2-4 | `verify_hypothesis.py`（新建）：校准闭环工具，扫描actual_outcome，计算误差，--commit标记verified |
| 修复 | `run_macro_analysis.py` argparse bug：`--hypothesis-severity` 从互斥组移出 |
| 修复 | `system_prompt_hypothesis.md` 全面更新：加入[PATHS]引用规范，量化区间需标注来源 |
| 文档 | AI说明文档/人类说明文档/README.md/KB_UPDATE_GUIDE.md 全面更新至 V2.0 |
| 验证 | 端到端测试通过（台海L2推演，置信度0.66🟡，PATHS引用正确，来源标注齐全）|

---



| # | 内容 |
|---|------|
| 新建 | `hypothesis_engine.py`：ScenarioParser / Wiki检索 / 历史类比 / RAG查询 / 提示词构建 / 报告保存 / Step 9 异步Wiki编译（280行）|
| 新建 | `hypothesis_templates.yaml`：10别名 / 6类情景 / 3级烈度 / 协同规则 |
| 新建 | `system_prompt_hypothesis.md`：假设推演 LLM 输出模板（置信度信号灯硬约束）|
| 新建 | `data/scenario_wiki.md`：情景知识库初始化 |
| 新建 | `~/hyp` 快捷脚本：NAS SSH 后直接输入情景名 |
| 修改 | `run_macro_analysis.py`：`--hypothesis` / `--hypothesis-severity` 参数 + 推演分支 |
| 修改 | `ntfy_listener.py`：`hypothesis` 指令解析 |
| 修复 | B1-B6 六项 bug（sp500正值/severity未传递/关键词不足等）|
| 验证 | 端到端冒烟测试通过（台海冲突升级 L2 全流程）|

---

## ✅ 本次已完成（2026-05-23 NAS Docker 端到端验证）

| # | 内容 |
|---|------|
| 重建 | `requirements.txt` 同步（新增 akshare/scikit-learn/plotly/anthropic 等），Docker 镜像重建完成 |
| 验证 | `akshare 1.18.63` 安装成功 |
| 修复 | `docker-compose.yml`：知识库挂载 `:ro` → 可读写（KB报告回纳需要写权限） |
| 修复 | `run_macro_analysis.py` line 475：FRED 代理 URL 双重 `http://` bug（`proxy_host` 已含 scheme，不应再拼接） |
| 同步 | 修复后的 `run_macro_analysis.py` 已同步到 NAS |
| 验证 | NAS Docker 端到端：`china --force` 通过（FRED代理切换成功✅ KB回纳成功✅ 报告生成✅） |

---

## ✅ 本次已完成（2026-05-23 V1 归档 + NAS 部署）

| # | 内容 |
|---|------|
| 归档 | `世界推演系统_V1_2026-05-23.zip`（620文件，3.96MB）桌面存档 |
| 同步 | 核心代码 → NAS：新增5个/更新10个/清理3个残留目录；NAS 现有 17 个 .py |
| 同步 | 知识库/财经知识库 → NAS：556文件全量同步，数量验证一致 |
| 修复 | `docker-compose.yml`：新增 `知识库` 卷挂载 + OUTBOUND_PROXY/OLLAMA_URL/NTFY_TOPIC 环境变量 |
| 修复 | `entrypoint.sh`：cron 环境变量导出补全 |
| 修复 | `crontab`：新增 run_macro_analysis 三条日报任务 + verify_predictions 月度任务 |
| 修复 | `run_macro_analysis.py` line 3903：KB 回纳路径 `docs/财经知识库/` → `知识库/财经知识库/` |
| 迁移 | 18 个旧 KB 报告迁移至 `知识库/财经知识库/07_分析报告/`；删除 `docs/财经知识库/` |
| 验证 | 本地端到端：中国速报 + 美国速报均通过（FRED代理✅ KB路径✅ 报告生成✅） |

---

## ⚠️ 待用户确认

| # | 操作 | 说明 |
|---|------|------|
| 1 | 确认 `NTFY_TOPIC` | docker-compose.yml 当前值 `macro-scan`，按手机 ntfy 订阅的主题名修改 |

---

## ✅ 本次已完成（2026-05-23 换机收尾）

| # | 内容 |
|---|------|
| 修复 | `optim_config.py`：`KNOWLEDGE_BASE` / `GEO_EVENTS_LOG` 路径由 `docs/财经知识库/` 改为 `知识库/财经知识库/`（两常量目前未被任何文件导入，但路径已订正） |
| 文档 | `说明书/定时任务手动启动.md` 全面重写：移除旧路径/ChromaDB/OpenClaw引用，补充 fetch 脚本、news.db、reasoning 参数及首次部署工作流 |
| 清理 | 删除 `核心代码/__pycache__/`（17个 .pyc，不随代码迁移） |

---

## ✅ 本次已完成（2026-05-23 文件整理）

| # | 内容 |
|---|------|
| 文档 | README.md 全面重写：文件数量修正为17个、data/目录结构、N1/N2/N3进度、数据来源表 |
| 文档 | `说明书/迁移指南.md` 全面重写：正确文件清单、完整环境变量、news.db验证步骤（2026-05-20→2026-05-23） |
| 文档 | `说明书/环境配置.md` 更新：移除 chromadb/langchain，精确版本锁定，新增 akshare/yfinance/dateutil |
| 文档 | `说明书/待修复清单.md` 替换为"系统状态"文档：17个文件状态+待完成工程任务 |
| 清理 | 删除 `核心代码/data/`、`核心代码/logs/`（路径修复前残留） |
| 清理 | 删除旧数据目录 `数据/`（已由 `data/` 取代） |
| 移动 | `数据/backtest_data/` → `data/backtest_data/`（4个回测结果 JSON） |
| 移动 | `核心代码/核心代码/data/mc_calibration.json` → `data/mc_calibration.json`（唯一副本，移至权威位置） |
| 移动 | `核心代码/docs/分析报告/` 旧报告（2026-05-21）→ `docs/分析报告/` |
| 清理 | 删除 `核心代码/核心代码/`、`核心代码/docs/`（路径 bug 产生的异常目录） |

---

## ✅ 本次已完成（2026-05-23 N1 新闻库第一阶段）

| # | 文件 | 修复内容 |
|---|------|---------|
| N1 | `news_db.py`（新建） | SQLite 持久化模块：6张表（scan_contexts/articles/article_categories/signal_episodes/episode_articles/signal_outcomes）+ 10个索引；url/content_hash 双路去重；pub_ctx_id 按发布时间插值；全部 CRUD 函数 |
| N1 | `scan_weak_signals.py` | `run_scan()` 集成：FRED本地CSV读取宏观值→写scan_context；文章全量入库+多类别打标；news_alerts触发标题注入（TOP-3）+signal_episode写入；全部操作 try/except 非阻断 |
| 验证 | 两文件 | `py_compile` 语法检查均通过 |

---

## ✅ 本次已完成（2026-05-23 OFAC代理修复）

| # | 文件 | 修复内容 |
|---|------|---------|
| Fix | `Crucix/apis/utils/fetch.mjs` | 新增 `shouldBypassProxy(url)`；`safeFetch()` 按 NO_PROXY 决定是否走代理 |
| Fix | `Crucix/docker-compose.yml` | NO_PROXY 新增 sanctionslistservice.ofac.treas.gov/amazonaws.com 等域名 |

---



| # | 文件 | 修复内容 |
|---|------|---------|
| Bug | `run_macro_analysis.py` | `_get_china_from_local_csv` 缺少 `from optim_config import DATA_DIR`，导致AkShare CSV数据静默失败；修复后PMI/PPI/工业增加值正确流入分析 |
| FixA | `run_macro_analysis.py` | 中国分析 `analysis_req` 追加数据锚点，禁止LLM幻觉（如将WTI价格编造成中国PPI） |
| FixB | `run_macro_analysis.py` | `format_news_for_prompt()` 新增 `country` 参数，中国模式自动为全球/本地新闻加标签；单国模式调用传 `country=country`；`both` 模式为中国报告单独生成 `cn_news_text` |
| 测试 | 本地端到端 | `--country china --force` PPI=-3.60%（修复前幻觉为9.82%）；月度简报无报错 |

---

## ✅ 本次已完成（2026-05-22 第九轮）

| # | 文件 | 修复内容 |
|---|------|---------|
| MiMo | `hybrid_llm.py` | 新增 `call_openai_compat()`：用 `requests` 直调 OpenAI 兼容端点（如 MiMo），无需新依赖；新增 `mode="openai"`；`auto` 模式检测到 `OPENAI_COMPAT_URL` 时优先走该路径 |
| MiMo | `run_macro_analysis.py` | `--reasoning choices` 加入 `"openai"`，支持 `--reasoning openai` 指定 |
| Bug | `run_macro_analysis.py` | `country=china/us` 单国模式下 `china_indicators`/`us_indicators`/`cn_recession`/`cn_inflation`/`mc`/`china_mc` 未初始化导致月度简报 `UnboundLocalError`；顶部统一初始化为空值；月度简报映射逻辑修正 |
| 测试 | 本地端到端 | `--country china --force --reasoning openai` 使用 MiMo v2.5-pro 全程跑通，LLM 叙事报告正常生成，月度简报正常生成 |

---

## ✅ 本次已完成（2026-05-22 第八轮）

| # | 文件 | 修复内容 |
|---|------|---------|
| E2 | `requirements.txt` | 全部 `>=` 改为 `==` 精确版本；补充 `python-dateutil==2.9.0.post0`（forecast_tracker 依赖）、`yfinance==1.3.0`（铜金比率扫描依赖） |
| D4 | — | 决定不做：datetime.now() 与 timezone.utc 各自在独立模块，无交叉比较；强改为 UTC 反而会破坏 FRED 新鲜度检查（naive/aware 比较 TypeError） |

---

## ✅ 上轮已完成（2026-05-22 第七轮）

| # | 文件 | 修复内容 |
|---|------|---------|
| F4 | `scan_weak_signals.py` | GDELT 失败时切换 Crucix 地缘关键词备份扫描；`_GEO_FALLBACK_KWS` 4类关键词；`fetch_crucix_news` 移到 GDELT 之前；except 块改为调用 `_gdelt_fallback_from_news(articles)` |

---

## ✅ 上轮已完成（2026-05-22 第五轮）

| # | 文件 | 修复内容 |
|---|------|---------|
| F5 | `fetch_china_data.py` | AkShare 接入：pmi_mfg / ppi_yoy / industrial_output 三条月频序列，带行数校验+NaN过滤安全护栏 |
| F5 | `run_macro_analysis.py` | `_get_china_from_local_csv` `_LOCAL_MAP` 新增 ppi→ppi_yoy、industrial_va→industrial_output |
| F5 | `scan_weak_signals.py` | `_CHINA_LOCAL_MAP` 新增 ppi→ppi_yoy、industrial_output→industrial_output |
| F5 | `requirements.txt` | 新增 `akshare==1.18.63`（版本锁定） |
| F3 | — | 中国本地 CSV 降级加厚：F5 补齐 pmi_mfg/ppi_yoy/industrial_output，fallback 逻辑早已在代码中，F3 目标完全满足 |
| A3 | `scan_weak_signals.py` | NeoData timeout 8s → 2s（NAS上无NeoData时快速失败） |

---

## ✅ 上轮已完成（2026-05-22 第四轮）

| # | 文件 | 修复内容 |
|---|------|---------|
| H1 | `regime_detector.py` | `_REGIME_CACHE` 改用 OPENCLAW_WORKSPACE，体制历史数据持久化 |
| H2 | `monte_carlo_v2.py` | 波动率校准文件路径改用 OPENCLAW_WORKSPACE，校准参数终于能被读取 |
| H3 | `run_macro_analysis.py` | `OLLAMA_URL` 改用 `os.environ.get`，支持 docker-compose 覆盖 |
| H4 | `forecast_tracker.py` | `dateutil.relativedelta` 改为顶部正常 import，两处 `__import__` 调用清除 |

---

## ✅ 上轮已修复（2026-05-22 第三轮）

| # | 文件 | 修复内容 |
|---|------|---------|
| G1 | `scan_weak_signals.py` + `run_macro_analysis.py` | `latest_news.json` 改用持久化路径（DATA_DIR / BASE_DIR） |
| G2 | `scan_weak_signals.py` + `run_macro_analysis.py` | 新闻/报告归档改用 WORKSPACE/BASE_DIR，不再写容器临时层 |
| G3 | `scan_weak_signals.py` | GDELT cache 加 7 天年龄检查，过期则跳过地缘调制 |
| F2+D2 | `run_macro_analysis.py` | 删除 CRUCIX_API 常量、`get_crucix_data()` 函数及死代码调用 |
| D3 | `optim_config.py` + 两个主入口 | makedirs 移入 `ensure_dirs()`，不再在 import 时自动执行 |

---

## 🟡 离线可做（本机改完，robocopy 同步后自动上线）

> 已清空 — 所有离线任务均已完成。

---

## 🟠 回局域网处理（NAS操作）

> ⚠️ **顺序要求：先执行 robocopy，再做任何 NAS 直接修改。**
> robocopy 使用 `/XO`（只覆盖旧文件），若先在 NAS 上改文件再同步，NAS 文件时间戳更新会导致本地修复被跳过。

| # | 任务 | 操作 |
|---|------|------|
| 1 | robocopy 同步代码 | 见下方命令（先做） |
| 2 | robocopy 同步知识库 | 见下方命令（先做） |
| 3 | docker-compose.yml 加环境变量 | `OUTBOUND_PROXY=http://192.168.31.108:7890`（FRED走代理）；`OLLAMA_URL=http://192.168.31.56:11434/api/generate`；确认 `NTFY_TOPIC` / `FRED_API_KEY` 已配。**注意：不要设置 HTTP_PROXY/HTTPS_PROXY，否则 AkShare 访问国内站点也会走代理** |
| 4 | 端到端测试 | `python run_macro_analysis.py --country both --force` 验证报告写入 /workspace/docs/ |

**robocopy 命令（步骤1-2，先执行）：**
```
robocopy "C:\Users\I327394\Desktop\世界推演系统\核心代码" "S:\macro-scan\核心代码" /E /XO
robocopy "C:\Users\I327394\Desktop\世界推演系统\知识库\财经知识库" "S:\macro-scan\知识库\财经知识库" /E /XO
```

---

## 🔵 较大工程（后续）

| # | 任务 | 说明 |
|---|------|------|
| E1 | Dockerfile + docker-compose.yml 完整模板 | 明确环境变量、卷挂载、healthcheck |
| N2 | **新闻库第二阶段**（反向查询） | 前提：N1 上线 ≥180天（即 **2026-11-19** 后）|
| N3 | **新闻库第三阶段**（信号校验） | 前提：N1 上线 ≥1年（即 **2027-05-23** 后）|
| M1-4 | ACLED武装冲突事件接入 | 前提：在 acleddata.com 注册获取 API key |
| M2-4激活 | 校准闭环（verify_hypothesis --commit） | 前提：真实地缘事件发生后，填写 actual_outcome |
| M3 | 推演后验准确率 ≥60%（≥10条样本）后开放 | 开放量化区间；日常报告情景接近度联动 |

---

## 🟢 长期规划

| # | 任务 | 说明 |
|---|------|------|
| 8 | 可视化仪表盘 | dashboard.py，不影响模型质量，最后做 |

---

## ✅ 历史已完成

| 2026-05-23 | N1 新闻库第一阶段：news_db.py（6表+10索引+CRUD）+ scan_weak_signals.py run_scan() 集成（宏观快照/文章入库/打标/触发标题注入/signal_episode写入），语法验证通过 |
| 2026-05-23 | OFAC代理泄漏修复：fetch.mjs shouldBypassProxy + docker-compose NO_PROXY扩展 |
| 2026-05-22 | F3 完成：F5 补齐数据源，fallback 逻辑已在代码中；A3 完成：NeoData timeout 8s→2s |
| 2026-05-22 | G1: latest_news.json 持久化；G2: 新闻/报告归档持久化；G3: GDELT cache 年龄检查；F2+D2: CRUCIX 死代码清除；D3: makedirs → ensure_dirs() |
| 2026-05-22 | F1 欧日 FRED 序列接入：`get_global_context()` 修复3个失效series，新增 ECB利率/欧元区GDP，CPI/GDP改用YoY%，加900天新鲜度过滤；`fetch_fred_history.py` 新增9条欧日英历史序列 |
| 2026-05-22 | **批量修复**：A1/A2/A4/A5（路径）；B1/B2/B4/B5（稳定性）；C1/C2（磁盘/日志）；D1（FRED key 环境变量化） |
| 2026-05-22 | run_macro_analysis.py：路径改 OPENCLAW_WORKSPACE；ntfy；降级标注；FRED代理环境变量化 |
| 2026-05-22 | forecast_tracker.py：BASE_DIR 改用 OPENCLAW_WORKSPACE |
| 2026-05-22 | regime_detector.py：VIX 键名兼容（VIXCLS/VIX） |
| 2026-05-22 | monte_carlo_v2.py：VIX 键名兼容；run_monte_carlo_compat() 多处 bug 修复 |
| 2026-05-22 | VIX 条件性危机参数（lambda_jump×3, theta×0.5）实现并验证 |
| 2026-05-22 | 本地版 OLLAMA_URL 改为 `192.168.31.56:11434/api/generate` |
| 2026-05-22 | NAS Docker 容器部署；fetch 脚本路径 Bug 修复（OPENCLAW_WORKSPACE） |
| 2026-05-21 | forecast_tracker 集成；铜金比率扫描；DRTSCIS 加入；MC mu_jump 单位修复 |
