# 世界推演系统 - Python 依赖
# 安装：pip install -r requirements.txt

# HTTP 请求（FRED API、GDELT、Gateway、Anthropic 直调）
requests==2.33.1

# FRED 数据拉取
fredapi==0.5.2

# 数值计算 / Monte Carlo
numpy==2.4.4
scipy==1.17.1

# 数据处理
pandas==3.0.2

# RAG 知识库检索（TF-IDF）
scikit-learn==1.8.0

# Dashboard 可视化
plotly==6.7.0

# Claude API SDK（hybrid_llm.py 的 anthropic 模式使用）
# 直调 Anthropic API（assess_structural_dimensions.py）只需 requests，无需此包
anthropic==0.89.0

# 日期计算（forecast_tracker.py relativedelta）
python-dateutil==2.9.0.post0

# 跨市场景气扫描（铜金比率，scan_weak_signals.py）
yfinance==1.3.0

# 中国宏观数据（PMI/PPI/工业增加值）
# 注：AkShare 爬取国内数据源，仅在中国大陆网络环境下可靠
akshare==1.18.63

# RAG 向量检索（可选；索引需在 Ollama 可达环境下预先构建）
# 构建索引：docker exec macro-scan python /app/build_rag_index.py
chromadb>=1.0.0

# RSS/Atom 解析（中文财经新闻源，fetch_rss_news.py）
# 离线安装：wheels/ 目录已预下载 feedparser-6.0.12 + sgmllib3k-1.0.0
feedparser==6.0.12

# Web UI 对话界面（Phase 3A，web_server.py）
fastapi>=0.100.0
uvicorn>=0.20.0

# YAML 解析（situation_tracker.py / synthesis_rules.yaml 等热加载配置）
# pyyaml 通常已随其他包安装，显式列出确保版本
pyyaml>=6.0

# Pydantic v2 数据契约（contracts.py I1 接口层）
pydantic>=2.0.0

# PDF 解析（ged_codebook_extract.py 解析 UCDP GED codebook PDF）
pypdf>=4.0.0

# NLP 实体识别（news_geo_feed.py 地名提取，P3-A）
# spaCy 3.8+ 兼容 numpy 2.x（3.7.x 有 thinc ABI 不兼容问题）
spacy>=3.8,<4.0
# zh_core_web_sm 改为名称+版本（2026-08-06）：pip 从 wheels/ 本地 wheel 安装（--find-links），
# 原 GitHub URL 构建时直连超时（NAS 出口 44KB/s）+ 代理不可用于 buildkit 网络
zh_core_web_sm==3.8.0

# PostgreSQL 驱动（worldsim-pg 统一采集库写入，A0.5）
# psycopg 3.x 二进制包（含 libpq 预编译 wheel，免编译）；从 tuna 镜像拉取
psycopg[binary]>=3.1.0
