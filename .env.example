# world-sim 环境变量总览
# 最后更新：2026-09-10（开源实施计划 阶段 1，CHG-20260910T223700-world-deduction）
#
# 本仓库为 monorepo，三个子系统各自读取自己的 .env，需分别配置：
#
#   macro-scan/.env     ← 天枢（观测采集 + 分析调度），变量最多，**建议先配这个**
#   macro-sim/.env      ← 天璇（演化仿真）
#   macro-ji/.env       ← 天玑（验证层）
#
# 配置方式：
#   cp macro-scan/.env.example macro-scan/.env
#   cp macro-sim/.env.example  macro-sim/.env
#   cp macro-ji/.env.example   macro-ji/.env
#   然后按各文件内的注释逐项填写。
#
# 必读提示：
#   1. .env 均已被 .gitignore 覆盖，不会进入仓库 —— 请不要把真值写进 .example 文件。
#   2. .env 是 docker compose 读取的注入源；改动后需 `docker compose up -d --force-recreate`
#      才会生效（`restart` 不会重新读取 .env）。
#   3. 各变量标注了 [必需] / [可选]：必需项缺失会导致系统起不来或核心功能失效，
#      可选项缺失只是对应数据源跳过，不影响主流程。
#   4. 本系统原部署在内网环境，部分默认值（如代理地址）指向私有网络，
#      在你自己的环境里需要覆盖，或留空走直连。
#
# 另见：
#   - 快速开始 / 部署步骤：见根 README.md
#   - 知识库搭建：见 docs/KB_SETUP.md（开源版不含知识库数据，需自行构建）
