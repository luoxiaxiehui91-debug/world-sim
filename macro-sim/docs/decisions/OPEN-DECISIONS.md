# OPEN-DECISIONS — 天璇未决项登记册

> 规范：只追加 + 就地关闭（OPEN → RESOLVED 补 Resolution）。每次 Phase 开始把未决项复现到工作上下文最前面。

| Date | Source | Open Item | Related Constraints | Current Leaning | Blocked By | Resolves When | Status |
|------|--------|-----------|---------------------|-----------------|------------|---------------|--------|
| 2026-08-08 | 探针（v2.0.28 C1-1a/C3-3a 50步标定探针） | liquidity_premium 死变量：m_v=0.0（50步 median\|delta\|=0），active_rate=0.327，探针标记 dead=True | C1-1a 探针机制：m_v<δ_min(0.002) → 死变量走 C3 不标定；但 C3-3a 刚移除 outflow，liquidity 又死，需判定是引擎无驱动还是clamp/scale 压制 | 倾向：先查引擎驱动链（A2/A3/A5/A10/A11/A12 对 liquidity 的写 + natural_decay ×0.93 是否把 delta 压到 <0.005），再决定走 C3 移除还是放大驱动 | 需人工复核引擎 liquidity 驱动链 | 引擎驱动链复核后 | OPEN |
| 2026-08-08 | A+D 修复批次探针复测（v2.0.29） | **liquidity_premium 仍死**：A=1.0+D=0.25 后 m_v 仍 0.0（seed 42/7/123 三轮全 dead）——A/D 只作用于 sentiment 路径（apply_sentiment_delta 前乘），liquidity 走 else 分支不经 MONTHLY_SCALE | 终局裁决：liquidity cap 假收敛（驱动过猛冲 +1.0 锁死，29/50 步 cap，cap 处 post-clamp=0 → m_v=0） | 需引擎 liquidity 驱动链复核（原 OPEN 项承接）；A/D 批次不覆盖 | 引擎驱动链复核 | 驱动链复核后 | **RESOLVED（v2.0.30 P0-1 dead 判定修正：clamp_frac 0.53-0.88 揭示 cap 饱和非真死，三 seed 全 dead=False，C3 移除不再适用，保留校准价值；clamp 对称后仍钉 cap=正写者 7 处主导（arch 预判应验），残余一致率问题转"接受线未达"项跟踪）** |
| 2026-08-08 | A+D 修复批次探针复测（v2.0.29） | **EASE 定向探针 ship 闸 FAIL**：独立子探针（初始 credit=0.3）50 步决策层 EASE=0——tightening 从 step 3 锁死 0.97 恒不满足 ease_signal 三条件（spread<200 ∧ tightening<0.3 ∧ grv_stress<0.3） | 终局裁决另立 issue ③：TIGHTEN +0.25 vs EASE −0.18 + [0,1] clamp + ×0.97 衰减（恢复比收紧慢 ~3 倍）→ 一旦收紧回不来 | EASE 判据降级：校准不阻塞，ship 阻塞。拆三条件完成（tightening 锁 0.97 为主障碍，非 spread/grv_stress） | 另立 issue ③ 修复（TIGHTEN/EASE 对称化） | issue ③ 修复后重测 | **RESOLVED（v2.0.30：ease_signal 放宽（spread<250/tightening<thr×1.0/grv<thr×0.5）+ 幅度对称 -0.18→-0.25 + EASE 后 2 步冷却；子探针按 QA R1 重构为两级能力验证——①合成 ctx 决策层 EASE_CREDIT ②宽松窗口写层落地 13/15、13/14、13/15，三 seed ship 闸全 PASS。原压力窗口 FAIL 为环境挡死（visible hf SHORT→tighten 先判）非引擎能力缺失）** |
| 2026-08-08 | A+D 修复批次探针复测（v2.0.29） | **接受线未达**：加权一致率 0.51-0.55 < 60%（sentiment ~0.55 改善但未过 / credit 0.55-0.61 边缘 / liquidity 0.33-0.50 仍死） | 终局裁决回退阶梯：A→探针→Guard A 失败→D=0.25→仍败→上报 ε_act 团队决议 | A=1.0+D=0.25 已实施为当前状态；sentiment 0.385→~0.55 显著改善，方向正确 | ε_act 团队决议（含 liquidity 驱动链） | 团队决议后 | **OPEN（v2.0.30 复测更新：加权 median 0.55 < 0.60 仍未达——sentiment 0.567/n=18、credit 0.55/n=40、liquidity 0.471/n=17，EASE ship 闸已 PASS、dead 判定已修正。候选第 2 轮=arch P0-1b pre-clamp 引擎意图 delta 测量（snapshot 已有 delta 字段，calibrator.py 一行切换，post-clamp 差值 cap 处恒 0 是测量盲区根），实施前需三方确认口径）** |
| 2026-08-08 | arch-review R4 | clamp 对称化连带 c)（阻尼锁区 \|level\|>0.7 重标）核实：三个核心文件无独立阻尼锁区检测，仅有 world_state.py:259 yen_carry_bleed_threshold=0.7（出血规则非锁区）——连带 c 实际不适用 | 评审基于假设，代码核实推翻 | 不实施连带 c | 无 | 已核实 | RESOLVED（不适用） |
| 2026-08-08 | 探针（v2.0.28 50步） | 一致率全部 <60%（sentiment 0.44/credit 0.48/liquidity 0.50）→ 决策树指向禁动 compute_error 查假收敛/阻尼，而非调权重 | data R4 决策树：新一致率也<60% → 禁动 compute_error | 下一步按决策树查假收敛/阻尼（可能 clamp 对称化后 GM 负向写入生效改变行为） | 需人工跑诊断对比 | 查假收敛/阻尼后 | **RESOLVED（决策树已执行：v2.0.30 clamp 对称后 liquidity 仍钉 cap（clamp_frac 0.53-0.88）证实测量盲区是根——post-clamp 世界差值在 cap 处恒 0 把"引擎疯狂驱动"误报"无驱动"，指向 arch P0-1b pre-clamp 引擎意图 delta 测量，见"接受线未达"项）** |
