# 天璇操作日志 2026-08-18

> 文档类别：实录（RECORD）· append-only · 天璇/验证层操作实录（按日追加）
> 目录约定：`macro-sim/docs/operations/YYYY-MM-DD.md`；版本权威见 `macro-sim/CHANGELOG.md`。
> 验收口径红线：一切验收以**容器部署后实测**为准。

## 摘要

| 时段 | 操作 | 关键 commit | 结果 |
|------|------|-------------|------|
| 06:3x-08:0x | 预测描述清晰化 + 判据回填 + L1/L2 自动验证 | `10fdf202`→`4d22e6e2` | 48 条全带判据；L1 FRED + L2 新闻判定器上线（见 CHANGELOG v2.0.41） |
| 08:0x-08:3x | 文档落地整理（neat-freak） | `780b3aa7` | STATUS/AGENTS/roadmap/CHANGELOG 三天大版本同步 |
| 13:0x-13:3x | **审计修复批次（world-sim-audit-20260818）** | `98e12e9f` | 15 项修复 + 登记表（见下） |
| 13:3x | **P0-1 天玑 env 恢复** | 本批 | 验证层恢复，healthcheck 强化为真实 PG 查询 |

## 审计修复批次（13:0x-13:3x）

**来源**：`C:\Users\luoxi\WorkBuddy\世界推演系统\world-sim-audit-20260818(-rev2).md`（5 路并行只读 agent + 主理人 SSH 实测复核）。登记表：`docs/decisions/audit-todo-20260818.md`。

**修复清单（容器实测）**：
- P1-1 L2 判定器死门禁（main 循环放行 L2 键）——门禁 9/9 实测
- P1-2 开阳 due_at 早 8h（daysLeft 显式 UTC）——bundle `index-14H7YVQ2.js` 部署
- P1-3 03_b0_schema.sql 补 action_key/human_note（迁移留痕）
- P1-4 时区硬化 10 处（天枢 5 热挂载 + 天玑 5 随重建）
- P1-5 weight_update_log.updated_at 补写入
- P1-6 macro-scan compose 补 networks + 3 变量（config VALID）
- P1-8 控制面 verify UPDATE 加 status 守卫（幂等 409/400 复测）
- P1-a macro-sim `ENV TZ=Asia/Shanghai`（实测容器 date=CST）
- P1-c/d regime until 校验 + 空档告警（单测 06→current / 10→next_alliance / 08 空档回退）
- P0-4 ntfy_listener SQLite 分支文件缺失即 return
- P2 死代码清理（sim_log/backtest/_write_json/挂载）

**复核修正**：verify_human.py 存在于 macro-sim（审计"不存在"有误）；backfill 容器存在 = docker cp 手动拷入（Dockerfile 已补 COPY）；P1-b 降级（_PG_ONLY 下 NoopConn 吞 qmark 非 psycopg 报错）；P1-2 建议 parseTs 契约冲突不可复用（按 UTC 语义显式修）。

## P0-1 天玑 env 恢复（13:3x）

**根因**：天玑容器 `WORLDSIM_APP_PW` 空（08-17 force-recreate 时 compose `${WORLDSIM_APP_PW}` 无 .env 提供 → 注入空）→ `get_connection()` raise → 验证层静默停摆（healthcheck 仅 import 模块 → healthy 假象）。

**修复**：
1. NAS 侧生成 `macro-ji/.env`（WORLDSIM_APP_PW 取自 `/vol2/1000/software/worldsim-pg/connection.env`，chmod 600，**值未落 git/记忆**；根 .gitignore 加 `**/.env`）
2. compose healthcheck 强化：`import tianji_db; get_connection()`（真实 PG 查询，env 空即 unhealthy）
3. `docker compose build && up -d --force-recreate`（顺带加载天玑侧 P1-4/P1-5 修复）

**验收（容器实测）**：
```
PW_len=32 ✅ | 容器 healthy（真实 PG healthcheck）✅
get_connection() → SELECT count(*) FROM predictions = 73 ✅
```

**遗留**：P0-2 明文密钥入 git（用户暂缓）；GED 补强恒 None（P2 门控，探针 bad=1 即此）；双 tianji_db 统一（下次大重构）。

## 修复后全量检测（13:3x）

- 探针 `silent_failure_probe.py`：31 项，bad=1（GED 已知 P2，非新问题）
- `verify_geo_auto.py --dry-run`：48 条待人工、0 到期（2027-02 首轮），主循环正常
- 天璇：`date` = CST ✅、`/app/backfill_criteria.py` ✅（Dockerfile COPY）
- `verify_human.py --list`：48 条 ✅（人工验证链路完好）
- 控制面：幂等 409 / 参数 400 复测 ✅（P1-8）
