# 天璇操作日志 2026-08-19

> 文档类别：实录（RECORD）· append-only · 天璇/验证层操作实录（按日追加）
> 目录约定：`macro-sim/docs/operations/YYYY-MM-DD.md`；版本权威见 `macro-sim/CHANGELOG.md`。
> 验收口径红线：一切验收以**容器部署后实测**为准。

## 摘要

| 时段 | 操作 | 关键 commit | 结果 |
|------|------|-------------|------|
| 10:5x | 调试脚本清理（NAS /tmp 36 + 天枢容器 /tmp 33） | 本批 | 清理完成，系统文件零触碰 |

## 调试脚本清理（10:5x）

**背景**：08-18/08-19 多轮调查（GDELT scale / 分数校准 / LLM 链路 / L2 门禁）产生 69 个临时调试脚本散落 NAS `/tmp` 与天枢容器 `/tmp`，经用户确认后清理。

**清理清单**：
- NAS `/tmp`（36 个，TSX 所有）：`annotate_grv.py` / `calib_s1.json` / `fill_llm_cfg.py` / `fill_oc.py` / `llm_config_backup.json` / `probe_{climate,dates,delta,dims,grv77,grv77b,grv77c,grv96,impact,llm_cfg,llm_get,llm_get2,recent,scale,scale_dist,seismic,sim}.py` / `recalc_{final,final2,history,scores}.py` / `restore_llm.py` / `test_{post2,prefill,put,regime,retry,translate,wti}.py` / `verify_{grv77,l2gate}.py`
- 天枢容器 `/tmp`（33 个 `.py`，同源；无 json）

**保留（不属调试产物）**：`wheels/`（pip 安装依赖）、`mneme-health-locks/`（mneme 健康锁）、`systemd-private-*` / `trim` / `_MEIgLoUxt` / `clr-debug-pipe-*` 等系统目录与锁文件。

**验证**：删除后两目录重扫，TSX/Users 残留文件为零；容器非目录残留为零。

**备注**：`llm_config_backup.json` 已冗余——`config/llm_config.default.json` 模板已入库 git，运行区配置已重建。
