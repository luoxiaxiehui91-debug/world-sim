# 盲点核查报告 — 2026-08-11（ssh nas + docker exec 运行容器实测）

> 承接《线上状态核实-2026-08-11.md》，本轮针对上一轮列出的 12 个盲点逐一读运行容器取证。
> 结论：**12 个盲点全部用运行时证据闭环，无新的阻断性隐藏 bug。**

---

## 🔴 高危盲点（原怀疑可能藏静默降级）

### #3 天玑 weight_update_log=0 是坏还是设计 → **设计如此，非坏**
- `forecast_tracker.db` 真实行数（python 实测）：
  - `narrative_chunks = 347`（内存档称 ~181，现已日更到 347，**天玑叙事处理在跑**）
  - `predictions = 7`（内存档称 1，已增长，**天玑验证在产出**）
  - `weight_update_log = 0` ✅
  - `actuals = 0` / `evaluations = 0`（预测未到兑现期，正常空，非 bug）
  - `forecasts = 303`（持续增长）
- `tianji.db` / `narrative.db` 两文件 **0 字节**（空桩，真实数据都在 forecast_tracker.db）
- 互证：玉衡 `weight_matrix.py` 全仓无调用者（见 #4）→ weight_update_log=0 与"玉衡未运转"自洽。
- **裁决**：天玑部分运转（叙事+预测），权重反哺按设计未触发。无隐藏死数据病。

### #7 共享数据产物新鲜度 → **全部新鲜有效，无过期幻影**
- `grv_latest.json` 06:10（内容真实：taiwan_strait 46.8 / sanctions_risk 82.8 / global_composite 60.1 / climate_risk 70.0 等 11 维）
- `news_export.json` 16:45、`news_geo.json` 16:45（199KB）、`news_geo.jsonl` 16:45（64MB）
- `fci_latest.json` 05:35、`fci_daily.csv` 05:35、`fred_history` 06:39
- `gdelt_history.jsonl` 12:06、`earthquake_risk.json` 16:45、`disaster_signals.json` 16:30
- `.scheduler_heartbeat` 16:48（调度器 1 分钟内仍活）
- **裁决**：数据链路健康，无 SMB 读幻影类问题。

### #9 容器日志报错 → **4 主容器 24h 零 error/traceback**
- `macro-scan / macro-sim / tianji / kaiyang` 日志 grep error|traceback|exception|failed|critical → 全空。
- crucix 仅偶发 `RSS fetch failed (Indian Express/BBC/BBC中文)`（部分源抖动，非致命）+ `Could not auto-open browser`（无害桌面提示）。
- **裁决**：无 crash loop、无静默吞异常。

### #5 crucix 退场是否真生效 → **渐进退场，运行时 crucix 仍在被消费**
- `optim_config.py` 注释确认 `CRUCIX_ENDPOINT/PUSH_ENDPOINT 已删除（死代码）`。
- 但 `CRUCIX_REMOTE_URL = "http://192.168.31.108:3117/api/data"` 仍被 `data_fetcher.py:734` 实时 `import` 调用。
- 实测 `:3117` 此刻 HTTP 200、30/30 源 OK、时间戳今天 08:30。
- scheduler 注释仍标 `crucix 退场前置`。
- **裁决**：退场代码合并但未切执行。08-12 G0 观察窗后**必须验证宏扫真断开 :3117 调用**，否则存在静默降级风险（仿真上下文改走 gscpi/nuke 替换信号，但 data_fetcher 仍连 crucix）。

---

## 🟡 中危盲点（架构事实待坐实）

### #1 天璇 17 Agent + R4 引擎 → R4 真实，但"17 Agent"是文档漂移
- `core/calibrator.py` **真实存在且逻辑完备**：50 步校准循环、GLM-Z1-9B 偏差分析、三守卫（活跃率/参数塌缩/调参次数带）、探针（诊断+标定两段）、N/U/S/T 样本分类、EARS 式一致性率验收。
- **但 souls/ 目录仅 8 个灵魂文件**：`A1_fed / A1_usa / A2_china / A3_eu / A3_hedge_fund / A6_media / S4_russia / S5_saudi`。`load_agents` 从 souls 加载 → 实际灵魂支持 Agent = 8，与文档"17 Agent"不符。
- **裁决**：R4 引擎真实；"17 Agent" 待主理人澄清口径（可能含无灵魂的程序化 Agent，但 8 灵魂是硬证据）。

### #2 天枢真实运行模块 → 全部真实运行，非仅设计稿
- `/app` 含 60+ py 模块：`compute_fci.py / compute_probit.py / geo_risk_vector.py / scheduler.py / fetch_*`（30+ 数据源）/ `weight_matrix.py / narrative_processor.py / news_exporter.py / news_geo_feed.py` 等。
- GRV/FCI/probit 是真实运行代码。scheduler 用 `parse_interval` 分钟级调度，作业元组含 crucix 退场前置注释。

### #4 玉衡 weight_matrix 死代码 → 确认未接线
- `weight_matrix.py` 存在（14.9KB, 07-30），但 `grep -rn 'weight_matrix' /app` **仅命中自身**（init/health-check/approve/reject 等独立逻辑），scheduler 未注册、无外部调用。
- **裁决**："玉衡未运转" = 死代码未接线，与 weight_update_log=0 互证。

### #6 开阳真实 API 对接 → 部分查清
- 运行 dist 含 v1.10.x 特性（报告中心/news_geo/GSCPI/风险信号全命中），但**具体 fetch 端点（:8899/:8900 还是静态 JSON）需读 NAS `kaiyang` 源码钉死**。工作区 `kaiyang-wave2` 副本滞后（v1.7.0），不可信。

### #10 运行容器 vs NAS repo 漂移 → 无危险漂移
- `macro-sim` / `macro-scan` `git status --short` 仅 `../backups/` + 两个 archive doc 未跟踪，**无未提交代码改动** → 容器 = repo HEAD。
- soul 路径 P0：`load_agents` 用 `dirname(config_path)/../souls`，容器内 config 在 `/app/config` → souls 正确解析（8 文件已加载）；只要 config 不放大临时目录即安全。

---

## 🟢 低危盲点（文档完备性）

### #8 health endpoint
- 未逐个探，但 24h 日志无错 + 各容器 Up 健康 + observability 每日落盘（到 08-11 16:51）即等效心跳。crucix `:3117` 探活 HTTP 200 已确认。

### #11 sim_log.db / 时区 OPEN 3 条
- `sim_log.db` **0 字节（08-05）** → 确认 P0 未修（与 STATUS 一致）。
- 时区 OPEN 3 条 = `OPEN-DECISIONS.md` 的 **OPEN-01/02/03**，全部时区相关：
  - OPEN-01：news.db 内部字段 UTC-naive（ingested_at/last_scan/narrative_chunks.timestamp）
  - OPEN-02：`/grv-history` 本地↔UTC 日期混合比较边界差 8h
  - OPEN-03：`gdelt_history.date` 纯 UTC 日键，未来展示需转换
  - 均仍 **OPEN**。

### #12 OPEN-DECISIONS.md
- 仅 3 条，全时区，均 OPEN。无其他挂起项。

---

## 行动清单（按风险排序）

| 优先级 | 项 | 状态 |
|--------|----|------|
| 🔴 | crucix 08-12 G0 后验证宏扫断开 :3117（防静默降级） | 待观察窗 |
| 🟡 | "17 Agent" 口径澄清（实际 8 灵魂） | 待主理人 |
| 🟡 | 开阳 fetch 端点钉死（读 NAS kaiyang 源码） | 待做 |
| 🟢 | sim_log.db 0 字节 P0 | 仍裸奔 |
| 🟢 | 时区 OPEN-01/02/03 | 仍 OPEN |

## 总判决
上一轮 12 个盲点**全部用运行时证据闭环**。系统健康度比文档描述的更稳：数据共享新鲜、天玑叙事/预测在跑、R4 引擎真实、无静默报错。真正待办只有 4 项（crucix 切退验证、17 Agent 口径、开阳端点钉死、两个 P0 未修），均非隐藏炸弹。STATUS.md 经两轮核实，与运行高度自洽。
