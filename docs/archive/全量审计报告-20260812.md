# world-sim 全量审计报告（2026-08-12）

> 范围：`S:\world-sim`（git 真相）+ NAS 实际部署（5 容器）。
> 方法：neat-freak 六事实面对账 + 当前事实矩阵；Phase 1 静态 5 agent + Phase 2 部署 3 agent 并行只读核实（1 agent 内部失败由主理人直查补齐）。
> 证据层级：所有结论基于 `ssh nas` / `docker exec` / git 实测，记忆仅作线索。今日=08-12，crucix G0 判定日。

---

## 一、六事实面状态总览

| 事实面 | 状态 | 关键结论 |
|--------|------|----------|
| 代码 | verified-current + 局部 mismatch | 17 Agent/8 soul 齐备、scheduler 53 JOBS 自洽；**sim_log writer 死代码**（比"零产出"更严重）；天枢 weight_matrix 残留副本 |
| 运行态 | verified-current + 局部 mismatch | 5 容器全活、crucix :3117 活且被消费、grv 契约一致、DB 行数基本吻合；**版本标签口径与文档不符** |
| 文档 | mismatch（多处滞后） | 版本号/计数/目录结构/权威性口径/双份 OPEN-DECISIONS/ADR 缺失 |
| 规则 | pending（未深审） | scheduler 令牌、import 白名单存在但未逐条验；本次未触及 |
| 记忆 | verified-current + 1 处 stale | SMB 红线已纠正（当前）；**sim_trigger P0 描述已过时**需更新 |
| 工作区 | mismatch（残留） | kaiyang-wave2 空壳/.bak/fci-recovery/HANDOVER/ignored 垃圾 |

---

## 二、当前事实矩阵（source-of-truth → stale-surface → action → verification）

### A. 文档滞后（改文档即可，P1/P2）

| # | 真相源（实测） | 过期面（文档） | 建议动作 | 验证 |
|---|---------------|---------------|----------|------|
| F1 | 镜像标签 macro-scan=v7 / macro-ji 无 VERSION / kaiyang=nginx:alpine | STATUS L54 写 v3.8.17/v1.0.0/v1.10.8 | STATUS 版本节加"镜像标签↔文本版本"映射，或在镜像固化 VERSION 文件 | B-容器 docker inspect |
| F2 | 天玑 predictions=7（E3 实测） | STATUS L96 写"当前 predictions=1" | 改 L96 为 7（L133 已对） | sqlite3 实测 |
| F3 | forecasts=304 | STATUS L133 写 303（+1 漂移） | 改 303→304 | sqlite3 实测 |
| F4 | repo 根实为 macro-scan/ macro-sim/ macro-ji/ kaiyang/；worldsim-review-synthesis.md 在 docs/archive；calib-* 在 docs/calib | 项目导航.md L17-31 写 世界推演系统天枢/… 且称这些在根 | 项目导航"文件结构"节重写或标注"工作区视角" | ls /s/world-sim |
| F5 | `S:\world-sim` 是 git 真相（用户 08-11 定） | 项目导航 L3-5 称"工作区是文档权威" vs STATUS L10-12 称"工作区读写不可信" | 统一口径：以 S:\world-sim 为真相；工作区=编辑副本 | 用户确认 |
| F6 | 两份 OPEN-DECISIONS 不同范围：macro-sim/docs（天璇校准 OPEN）/ macro-scan/docs（天枢时区 OPEN-01/02/03） | STATUS/项目导航 未说明有两份，统称 OPEN-DECISIONS | 合并为单册或保留双册但互链+注明范围 | diff 实测 |
| F7 | 全仓无 ADR-*.md（decisions 目录仅 OPEN-DECISIONS.md） | STATUS L29 引用 ADR-08 等 | 补 ADR 文档或更正引用 | Glob 实测 |
| F8 | repo 含 macro-ji+kaiyang；worldsim-review-synthesis.md 在 docs/archive | SOP.md L31/L53 称 repo 仅 macro-scan+macro-sim、T1 权威在根 | SOP 小修两处引用 | Read SOP |
| F9 | kaiyang VERSION=1.10.8，package.json version=1.9.0 | 版本多源不一致 | bump package.json（低危） | Read kaiyang |

### B. 实现偏离 / 代码问题（P0/P1）

| # | 真相源（实测） | 过期面（文档） | 建议动作 | 验证 |
|---|---------------|---------------|----------|------|
| F10 | sim_log.py `insert_run` 全仓 0 调用点；实际落盘走 forecast_tracker.db(run.py:482) | STATUS L98/L142 称"已接线但零产出，P0" | 修正定性=未接线死代码；或接线 writer | grep insert_run 实测 |
| F11 | macro-sim data 是目录挂载 `/app/macro_data`（非 sim_trigger 单文件 bind）；sim_trigger.json 0B mtime 8/6 | STATUS/MEMORY 称"单文件 bind+os.replace 断链 P0 仍裸奔" | MEMORY/STATUS 该 P0 描述更新（已非现状）；sim_log.db 才是单文件 bind（但 sim_log 死代码） | B-契约 docker inspect |
| F12 | 天枢 `核心代码/weight_matrix.py` 死代码残留副本（macro-ji 副本同体） | STATUS 定性死代码但未物理删 | 删天枢副本或标注（与"玉衡未运转"定论一致） | grep weight_matrix 实测 |

### C. 残留该清（P2，删除候选，待确认）

| # | 残留物 | 证据 | 处置建议 |
|---|--------|------|----------|
| F13 | `kaiyang-wave2/`（仓内空壳，仅空 dist/，未 git 跟踪，无活跃引用） | A-开阳 ls/grep/docker-compose 实测 | 删除候选（与 596780c 同类） |
| F14 | `STATUS.md.bak-20260811`（根目录，gitignored *.bak） | ls /s/world-sim | 删除或移 backups/ |
| F15 | `fci-recovery/`（根目录，Aug 10 建，来源不明） | ls /s/world-sim | 待定性（确为脚手架则删） |
| F16 | `HANDOVER.md`（根目录 20KB，MEMORY 注"停更降级快照"） | ls /s/world-sim + MEMORY | 确认是否保留 |
| F17 | macro-scan 核心代码 ignored 垃圾：NDH6SA~M / qa_result.txt / 世界推演系统_人类说明文档/ | C3 git status --ignored | 清（已被 ignore，无害噪音） |
| F18 | 天枢 50 未调度脚本中少数疑似停摆/替代源：news_geo_feed.py(8-11 停调度)、fetch_rss_news.py / fetch_china_data_akshare.py / fetch_sipri_backdrop.py(疑似被取代)、backfill_grv_history.py 等手动工具 | B2 静态清点 | 人工确认删/接线（不急） |

### D. 已确认对齐（无需动作，记录备查）

- 17 Agent(A1-A12+S1-S5) + 8 soul 文件，映射无断链（B2）
- 天玑=独立容器 macro-ji；玉衡权重未运转（B1）
- weight_matrix / tianji.db / narrative.db 死代码/空桩（C1，与 STATUS 一致）
- 5 容器全活；crucix :3117 活且仍被 data_fetcher.py:734 消费（D1/D4，G0 日未切，符合"观察窗中"）
- deploy.sh macro-sim COPY 重建、全仓 0 处 rsync --delete（D2）
- grv_latest.json 16+global_composite、bind 源 sha256 一致（E2）
- narrative_chunks=347 / predictions=7 / weight_update_log=0（E3）
- 天枢 scheduler 53 JOBS/49 名、无幽灵 Job①③、tianji_trigger 每日 09:42（B2/D3）
- macro-scan git 卫生干净、无 CRLF 幻影（C3）
- kaiyang-wave2 无源码重复；nginx 嵌套挂载已按 37dda5a 治本（C4）
- 开阳 VERSION/CHANGELOG 与 STATUS v1.10.8 一致、news_geo 源码齐全（B1）

---

## 三、分级发现汇总

### P0（阻断性）
- **无新 P0**。唯一曾标 P0 的 sim_trigger 单文件 bind 断链（F11）经实测**已非现状**（目录挂载），原描述属文档/记忆滞后，降级为 P1 修正描述。sim_log.db 单文件 bind 仍在但 sim_log 为死代码，无运行影响。

### P1（应修，文档/定性纠正）
- F1 版本标签口径混乱
- F2 STATUS predictions 内部自相矛盾（1 vs 7）
- F4 项目导航目录结构描述过时
- F5 项目导航 vs STATUS 权威性互相矛盾
- F6 双份 OPEN-DECISIONS 未说明
- F7 ADR 文档缺失
- F10 sim_log 定性错误（"已接线零产出"→死代码）
- F11 sim_trigger P0 描述过时（记忆+STATUS）

### P2（可缓，清理/小修）
- F3 forecasts 计数 +1、F8 SOP 引用、F9 package.json 版本、F12 天枢 weight_matrix 副本
- F13-F18 残留清理候选

### 待决/观察（不动）
- crucix 退场 G0 08-12 未切（D4，符合观察窗；任务 #7 待 G0 后验证）
- news_geo 验收 08-13 观察窗
- silence 治理挂起、FRED 上游停更、时区 OPEN-01/02/03（已登记）

---

## 四、清理候选清单（Phase 6 待确认，先预览不删）

| 候选 | 理由 | 风险 |
|------|------|------|
| kaiyang-wave2/ | 仓内空壳、未跟踪、无引用 | 低（同 596780c 已清类） |
| STATUS.md.bak-20260811 | 根目录 .bak 残留 | 低（gitignored） |
| fci-recovery/ | 来源不明脚手架 | 中（需先确认非在产） |
| HANDOVER.md | 停更快照 | 低（确认后可删） |
| macro-scan 核心代码 ignored 垃圾 | 噪音 | 低 |
| 天枢 少数停摆脚本 | 疑似替代/弃用 | 中（需人工判删/接线） |

**文档修正（落 `S:\world-sim` 推 GitHub，非删）**：F1/F2/F3/F4/F5/F6/F7/F8/F9/F10/F11/F12 属编辑，走 commit/push。

---

## 五、两阶段汇报（neat-freak 骨架）

**影响**：消除文档误导（版本号/计数/目录结构/权威性口径/双 OPEN 册/缺 ADR）、修正两处错误定性（sim_log 死代码、sim_trigger P0 过时）、清掉一批残留游离文件，使下次 session 冷启动拿到唯一现役答案。

**改动/新建（建议）**：
- 文档修订 F1-F12（11 处，编辑 `S:\world-sim` 后 commit/push）
- 清理候选 F13-F18（删除，待你确认）

**待你决定**：
- F5 权威性口径以哪份为准（建议 S:\world-sim 为真相）
- F6 双 OPEN-DECISIONS 合并还是保留双册互链
- F13-F18 删除范围（全清 / 只清低风险项）
- fci-recovery/ 是否确认非在产

**遗留**：crucix 退场 G0 判定（今日触发，待自动化结果）、news_geo 验收、silence 治理、FRED 停更、时区 OPEN-01/02/03 —— 均属已知观察项，非本次审计新发现。

---

## 六、审计过程备忘
- Phase 0 种子表：审计种子表.md（S1-S20）
- Phase 1 静态 5 agent（天枢/天璇/天玑玉衡/开阳/文档）；文档 agent 连败 3 次，主理人直查补齐
- Phase 2 部署 3 agent（容器/契约/数据）全成功
- 全部只读，未改动任何文件/容器
