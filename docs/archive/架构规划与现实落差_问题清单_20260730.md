# 世界推演系统 · 架构规划与现实落差 · 问题发现清单

> 整理时间：2026-07-30 | 系统版本：macro-scan v3.7.0（VERSION 文件实测）| 关联文档：信息源完整手册 / 架构总文档(16) / 天璇协议(17) / 慢变量盘点(18) / 预测目标体系(19) / prior.yaml 权重矩阵(20) / 终裁优先级(00) / 事实地基(01)
> 验证方式：①SSH `nas`→docker exec `macro-scan-macro-scan-1` 直读 `/var/log/macro-scan/scheduler.log` 与 `/workspace/data` 真实 mtime（实跑确认接入率）；②`/app/*.py` 代码级 grep 各数据源落地文件被哪些脚本引用（计算溯源）；③SSH 直读 `S:\20260729\` 全部 13 篇规划文档；④容器内核查 v3.7.0 天玑/玉衡/慢变量模块是否存在、是否调度、`prior.yaml` 能否被 `yaml.safe_load` 加载。
> 证据纪律：SMB 挂载一律不信任，所有读/写均走 SSH/scp，避免幻影。

---

## ⚠️ 零、对本清单初版的自我修正（务必先读）

本清单初版（文件名含"信息源利用"）曾下结论："约 10 类死数据是**被规划评估后跳过 / 纯浪费**"。

通读 doc16 / doc19 / doc20（prior.yaml 权重矩阵）并核容器实况后，**该结论判宽了，需收窄**：

- **doc18 的"跳过"只针对慢变量层（IRP/UCRI/GCI）**。FX 现货、加密、航运本就是**快变量**，本就不进慢变量，这没问题。
- 这些源的**预定消费者是"权重矩阵 → 天璇预测目标"这一快变量层**（见 doc19 目标类型 + doc20 prior.yaml 显式权重映射），不是被否定，是"**引擎还没建好，先囤着数据**"。
- 这恰好呼应 doc00 终裁 **T1-1「孤儿源抢救性积累，历史不可逆」**——先采，等天璇重写后能立刻有历史数据校准。

→ **正确定性**：这 10 类源不是"死数据/浪费"，而是"**预采集库存 + 消费侧尚未就位**"。但风险真实存在：若天璇重写与 prior.yaml 修复长期搁置，这批库存数据将永不成活。下面第一节据此重写。

**本清单第二节原标 🔴 P0（prior.yaml 损坏、08-01 必炸），但 14:24 复核已被推翻降级为 P2（休眠风险）**：prior.yaml 确实损坏，但运行路径全读 `grv_weights.yaml` 不读它，08-01 月度 job 不受影响。本清单真正紧急项实际为空，其余为 P1/P2 规划-现实对齐问题。

---

## 一、问题 P1：约 10 类源是"预采集库存"，消费侧未就位（原"死数据"重定性）

**定义**：每天调度 fetch 落盘成功、但**当前版本没有任何计算模块读取、也没有展示层呈现**的数据源。即"只采集、暂未计算、不展示"——但**在规划层（prior.yaml + doc19）已分配了消费者**，只是消费者（天璇重写引擎）尚未实现。

| # | 数据源 | 落地文件 | 每日调度 | 当前消费者 | 规划中预定消费者（doc19 + prior.yaml） |
|:--|:--|:--|:--|:--|:--|
| 1 | AkShare 全球宏观 | `world_history.jsonl` | ✅ 05:50 | ❌ 无 | 全球宏观/景气维度（快变量，待天璇） |
| 2 | Yahoo FX（汇率） | `fx_history.jsonl` | ✅ 05:55 | ❌ 无 | 6.5 汇率危机（prior.yaml 权重 0.40，USD/CNY 直接） |
| 3 | CoinGecko（加密） | `crypto_history.jsonl` | ✅ 06:00 | ❌ 无 | 8.4 加密崩溃（prior.yaml 权重 0.40） |
| 4 | Binance / Kraken（加密） | `crypto_extra.json` | ✅ 06:12 | ❌ 无 | 8.4 加密崩溃（辅助） |
| 5 | OpenSky（航班） | `airtraffic_opensky.json` | ✅ 06:28 | ❌ 无 | 7.3 航运中断 / 11.1 传染病（prior.yaml 0.35） |
| 6 | HDX（人道） | `hdx_risk.json` | ✅ 06:20 | ❌ 无 | 7.4 粮食危机 / 12.4 难民（prior.yaml 0.35/0.35） |
| 7 | FAO（粮价） | `fao.json` | ⏸ 月频 | ❌ 无 | 7.4 粮食危机（prior.yaml 关联） |
| 8 | AkShare 中观（二手房/景气） | `china_meso.json` | ⏸ 月频 | ❌ 无 | 景气维度（快变量，待天璇） |
| 9 | Stooq BDI（航运） | `bdi.json` | 🟡 06:25 本地 CSV 兜底 | ❌ 无 | 7.3 航运中断（prior.yaml 标 `frozen:true 0.05`） |
| 10 | Yahoo 商品（铜/WTI/Brent） | `commodity_yahoo.json` | ✅ 06:26 | ⚠️ 仅 `fetch_energy_eia` 读→EIA→narrative，间接 1 跳 | 5.x 能源 / 7.4 粮食（prior.yaml 0.30） |

**GRV 11 维的真实输入**（直接读 `geo_risk_vector.py` 的 `open()/read_csv` 确认）：GDELT×8 + FRED×3 + GPR×39 + OpenSanctions + USGS + UK Carbon/NREL/NG + GDACS + ONI/FIRMS。**上述 10 类源 GRV 一概不读**（这是**设计正确**的——它们本属快变量层，不该进 GRV 状态向量）。

**报告主入口 `run_macro_analysis.py`**：全文 grep `yahoo / coingecko / akshare(fx/commodity) / tushare` 全为空 → 报告既不读这些历史文件、也不 live 重拉 → FX/加密/商品在**当前报告**端到端未被使用。

**展示层 `web_server.py` / `dashboard.py` / `build_report_data.py`**：仅展示 climate / disaster / grv，build_report_data 只读 fred_history。**无 `glob/listdir` 通读 data 目录的兜底** → 这批库存数据连被看一眼的机会都没有。

**量化**：27 个外部源中，真进**当前版本**计算的约 12 类（FRED/GPR/GDELT/OpenSanctions/USGS/UK Carbon+NREL+NG/GDACS/ONI+FIRMS/Crucix/AkShare中国/news·RSSHub/EIA）；"预采集库存"约 10 类，占已连接源的 **~38%**。

> **与初版结论的差异**：初版称"纯成本零回报"，现修正为"**成本已发生，但计划内的回报（天璇消费层）尚未交付**"。问题从"该不该采"转为"**消费引擎（prior.yaml 修复 + 天璇重写）什么时候建**"——后者才是真瓶颈。

---

## 二、⚠️ 问题 P2（休眠风险，14:24 复核降级）：prior.yaml 语法损坏，但被运行路径完整避开

**现象**：容器 `/workspace/config/prior.yaml`（38351 字节）**已部署**，且正是 doc20 审核报告 P1 指出的**内联多键值错误格式**，例如：

```yaml
# 错误写法（同一行塞两个键，yaml 解析器无法识别）
"1.1": 0.05 "1.2": 0.05
```

**现场验证**：在容器内执行 `python3 -c "import yaml; yaml.safe_load(open('/workspace/config/prior.yaml'))"` —— **直接 traceback 崩溃**，文件根本无法被任何模块加载。

**调度实况（14:24 复核，原 P0 判定被推翻）**（读 `scheduler.py`）：三个月度 job 实际命令如下，**全部读 `grv_weights.yaml`、不读 prior.yaml**：

| job | 实际命令 | 读哪个文件 | 碰 prior.yaml？ |
|:--|:--|:--|:--|
| `slow_vars`（月1日0935） | `slow_variables.py` | 不引用 prior | 否（全代码 grep 证实 prior 仅出现在 weight_matrix.py） |
| `tianji_verify`（月1日0940） | `tianji_verifier.py` | `grv_weights.yaml`（有 `try/except:return 0.20` 兜底） | 否 |
| `weight_health`（月1日0945） | `weight_matrix.py --health` | `grv_weights.yaml`（`run_health_check`） | 否（`--health` 不碰 prior） |

→ **08-01 这三个月度 job 都不会因 prior.yaml 损坏而报错**。此外 `weight_matrix.py::init_weights_from_prior()` 首行即 `if os.path.exists(WEIGHTS_PATH): return`（WEIGHTS_PATH=grv_weights.yaml），故即便手动 `--init` 也会因 grv_weights 已存在而跳过 prior。原"slow_variables.json 不存在佐证必炸"系误判——slow_vars 本就不读 prior，其产出缺失另有原因，与 prior 无关。

**模块落地状态**（v3.7.0 已部分实现 doc16 设计，但权重数据坏了）：
- `/app` 存在：`slow_variables.py` / `weight_matrix.py` / `tianji_verifier.py` / `tianji_db.py` / `narrative_processor.py` / `fetch_defense_rss.py` / `fetch_sipri_backdrop.py`（与 AGENTS.md v3.7.0 模块群一致）。
- `/workspace/config/` 存在：`grv_weights.yaml`（20323B，正常）/ `prior.yaml`（38351B，**损坏**）/ `source_dimension_map.yaml`（2239B，正常）。
- `daily_narrative` 今日 07:00 已 FIRING 并产出 `daily_narrative_cache.json`（3189B）→ **叙事模块正常**，唯独权重数据链断了。
- **天璇 macro-sim 重写**（真正吃"权重矩阵 + 预测目标"的引擎）doc16 §3 明写"现有基本重写 / 未做" → 即便 prior.yaml 修好，消费端仍空着。

**影响（收敛）**：规划（doc16-20）与 v3.7.0 代码高度对齐，且**真正串起权重链的文件 `grv_weights.yaml` 是好的**（合法 dict、含 19 个信源完整权重）。prior.yaml 损坏**不影响任何当前推演产出**，也不影响财经线借用方案（L1 市场价格 FCI / L3 收益率曲线 probit 本就不依赖 prior.yaml）。它仅意味着"若未来要从 prior.yaml 重建权重矩阵，需先修格式"——属规划层 tidy-up，非线上缺陷。

**建议（非紧急，顺手 tidy-up）**：保留待办——可在下次维护窗口把 USGS 起 16 源内联多键改 block 格式（doc20 P1 方案 A）+ 补 BDI_Stooq 65 个 0.05 占位（P2），修完容器内 `yaml.safe_load` 复验。在此之前无需动作，也不进 P0/P1 优先级。

---

## 三、问题 P2：文档之间、文档与实现之间存在漂移

| # | 漂移点 | 详情 |
|:--|:--|:--|
| 1 | doc16 N8 自相矛盾 | N8「数据源与慢变量映射未盘点」标 🟢 阶段三，但 doc18（**同日 07-30**）已做完整盘点 → 文档内部打架 |
| 2 | BDI 口径三处对着干 | ①《信息源手册》记"BDI 🟡 本地 CSV 兜底运行中"（现实）；②prior.yaml 把 `BDI_Stooq` 标 `frozen:true 0.05`（陈旧"Stooq 不可用"）；③doc16 N12 要用 **Freightos FBX**（新航运指数）作真航运指数。三者未对齐 |
| 3 | 治理基调未显式对齐 | doc00（07-29 辩论终裁）基调"先查事实、冻结别急接"；doc16-20（07-30）基调"大步铺开 7 层架构"。doc00「不做清单」含"禁止静默补零接线"——prior.yaml 对 BDI 用 `frozen+0.05` 显式占位是合规安全做法，但整体文件损坏又触发另一种失败，两篇精神未打通 |
| 4 | 版本/维度漂移（已修记忆，手册部分未全改） | 代码 v3.6.5→**v3.7.0**、GRV 8 维→**11 维**；《信息源完整手册》顶栏已加实跑复核标注，但「五、时效性速查」仍把 BDI 列日频、「六、风险等级」仍标 BDI/ACLED 🔴 不可用（本轮未触及其它章节） |

---

## 四、问题 P3：两层权重易被混淆，需在文档/对接时显式区分

系统存在**两套互不相干的权重**，混淆会导致错误结论：

| 层级 | 存储位置 | 用途 | 消费方 |
|:--|:--|:--|:--|
| **GRV 状态向量权重** | `geo_risk_vector.py` 硬编码常量 | 11 维地缘风险状态向量的加权合成 | GRV 引擎（天枢内部） |
| **源×预测目标权重矩阵** | `prior.yaml`（当前损坏） | 各数据源对各预测目标(6.5/7.4/8.4/…)的贡献权重 | **天璇 macro-sim**（尚未重写） |

**关键澄清**：第一节那 10 类源"GRV 不读 FX/加密"的结论**始终成立且正确**——它们本就不该进 GRV 状态向量，它们的预定归宿是 prior.yaml 那张矩阵、供天璇预测打分。把"GRV 不读"误读成"系统完全不用"是初版清单的失误，已在本清单零节修正。

---

## 五、连带次要问题

1. **ACLED 始终未接**：edu/org/gov 邮箱门槛，自调研日起未接，规划也未打算接（doc18 已用 GDELT 替代）。属长期已知，非新发现。
2. **天璇重写是更大工程**：prior.yaml 修好只是打通数据链第一步，真正消费端（doc16 §3 的 macro-sim 重写）仍"未做"。这是 doc00/dco16 阶段二/三的范畴，不抢跑，但需有明确的里程碑闸口，否则 P1 的库存数据会继续空转。
3. **文档成稿时间差**：13 篇规划文档多在 07-29 深夜至 07-30 凌晨成稿，与代码 v3.7.0 几乎同步演进，但**文档之间未做交叉一致性校验**（见第三节），建议补一道"文档互审"关卡。

---

## 六、问题 P1（财经线专项）：市场价格层从未点亮（2026-07-30 容器实况核验）

> 配套深挖文档：《世界推演系统_宏观财经分析成熟架构对标_20260730.md》（含 6 类成熟范式机制 + 财经架构蓝图 L0-L5）+ 《世界推演系统_财经成熟范式机制与可借鉴点_20260730.md》（机制详解 + 可借鉴矩阵）。本节约记"实况 + 缺口 + 优先级"。

**核心结论**：财经推演**没有退化**，是**市场价格层（汇率/加密/大宗商品/航运）从未真正接入计算**。宏观财经线（FRED/GPR/GDELT 新闻话语）满血，但市场价格全暗。

**活着的（满血）：**
- FRED（VIX/BAA/T10Y2Y）→ `regime_detector` + `scorer` + 报告 live；
- GPR → GRV + 慢变量；
- GDELT 金融 NLP → weak_signal 中"日元87/标普69/USD68"密集（但来源是新闻文本，非价格文件）。

**休眠 / 死数据（已抓零消费，呼应第一节 #2/3/4/9/10）：**
| 源 | 落盘 | 消费 |
|---|---|---|
| Yahoo FX | 当日新鲜 `fx_history` | ❌ |
| CoinGecko / Binance / Kraken 加密 | 当日新鲜 | ❌ |
| Yahoo 商品（铜/WTI/Brent） | 当日新鲜 | 仅 EIA fetcher 间接 1 跳 |
| Stooq BDI 航运 | 当日新鲜 | 仅 commodity fetcher 交叉读 |

**结构性缺口：**
1. **GRV 11 维无市场价格维度**：仅 `japan_monetary`/`energy_grid`/`sanctions` 沾金融边，无 FX/加密/商品/BDI 维度 → **P1**。
2. **prior.yaml 语法损坏（P2 休眠，已复核降级）**：文件确实损坏，但运行路径不加载它（读 grv_weights.yaml），08-01 不炸、不影响当前推演；仅"未来从 prior 重建权重"时需先修格式。故**不阻断**市场价格层规划接线（详见第二节）。
3. **报告 `run_macro_analysis.py` 既不读价格文件也不 live 重拉**（grep 实证为空）。
4. **weak_signal → narrative 严重衰减**：金融词密（日元87），终稿叙事只剩"日元套利+FOMC"三两句。
5. **无 vintage / 修订纪律**：fetcher 覆盖写，无法诚实回测。

**对标可借鉴点（成熟范式"怎么做的"→ macro-scan"借什么"，详见深挖文档）：**
- **Chicago NFCI 自适应 PCA 权重 + 成分分解**：用已抓的 FX波动/加密回撤/商品异动/BDI + FRED 信用利差/VIX 造综合 FCI（z-score，零=常态，风险/信用/杠杆三分指数）→ **直接点亮死数据**（L1）。
- **NY Fed 新闻分解归因**：每条发布对指数的贡献 = 意外×载荷，可量化展示 → 根治叙事衰减（L5）。
- **收益率曲线衰退 probit**：把已进 regime 的 T10Y2Y 做成 `P(衰退)=Φ(α+β·利差)`（AUC 0.85-0.89）→ 廉价早期预警（L3）。
- **GDELT 三步 + PCA**：关键词→NN 过滤→按国家/月聚合→PCA 取 3 主成分喂简单 AR；**直接用 BDI+原油作控制变量**（macro-scan 已抓却闲置）。

**优先级建议（与本文其他节对齐）：**
- "市场价格层未点亮"= **P1**；prior.yaml 损坏已复核降级为 **P2 休眠（不阻断运行，见第二节）**；GRV 无市场维度 = **P1**；叙事衰减 = **P2**。
- **最廉价的两处即时借用（不依赖 P0 修复、不依赖新数据源）**：**L1 市场价格 FCI**（用现有死数据，零新源）+ **L3 收益率曲线 probit**（用现有 T10Y2Y，几行代码）。两者可在"先不修"定调下独立点亮财经线。

---

## 七、建议动作与优先级（待决策，本文未实施任何改动）

| 优先级 | 动作 | 性质 | 风险 | 说明 |
|:--|:--|:--|:--|:--|
| 🟡 **P2 顺手** | **修 prior.yaml（降级）** | 纯文件修复 | 零架构风险 | USGS 起 16 源内联多键改 block + 补 BDI_Stooq 65 占位；但运行路径已避开，**非紧急**，可下次维护窗口顺带，无需赶 08-01 |
| 🟠 P1 | **重定性库存源 + 设消费里程碑** | 文档/规划 | 低 | 把第一节"死数据"措辞改为"预采集库存+消费侧未就位"；为天璇重写设明确闸口（避免库存永空转） |
| 🟡 P2 | **文档漂移对齐** | 文档 | 零 | 修 doc16 N8 状态、统一 BDI 三处口径(Freightos/手册/prior.yaml)、补 doc00↔doc16 治理口径衔接 |
| 🟢 P3 | **手册旧章节同步** | 文档 | 零 | 《信息源完整手册》「五/六」节的 BDI/ACLED 旧标清理 |
| 选项A | 按 doc19 目标接入 | 代码+契约 | 中 | 把已抓数据接进对应预测目标管线（FAO→7.4、加密→8.4、FX→6.5…），需先升 scan 验证再升 sim（铁律） |
| 选项B | 停采省配额 | 调度 | 低 | 删除零消费源 fetch（免费额度本就用不掉）；但会丢失 doc00 T1-1「历史不可逆」的库存价值，**与规划方向冲突，不建议** |

> **本次结论（14:24 复核后）**：原 P0（prior.yaml 损坏致 08-01 必炸）**已复核推翻**，降级为 P2 休眠风险，不影响当前运行。真正待办为 P1（死数据收口/财经 FCI/GRV 市场维）+ P2（文档漂移对齐），按优先级有序推进即可。

---

## 附：本清单与"信息源利用问题发现清单_初版"的关系

- 初版文件（同名含"信息源利用"）结论"死数据=被评估后跳过/纯浪费"**判宽**，本清单予以**正式收窄修正**（见零节、一节）。
- 初版文件建议**废弃/由本清单取代**；若保留，请视为"已被本清单推翻的初判"，避免后人误读。
