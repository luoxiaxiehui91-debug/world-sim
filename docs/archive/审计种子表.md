# 审计种子表（Phase 0 — 主理人建基线）

> 提取自 STATUS.md / 项目导航.md 的「文档声称事实面」+ 即时结构扫描发现。
> 所有条目待 Phase 1(静态)/Phase 2(部署) 用实时证据重验；未验证标 pending。
> 读取源：NAS repo `S:\world-sim`（git 真相，Aug 12 06:2x 实测）。
> 约定：ssh 用 Git 自带 ssh；读运行时数据走 `S:\macro-scan\data`（仓库外），勿走 `S:\world-sim\macro-scan\data`（死副本）。

---

## 一、文档声称的可验证事实（待实时重验）

| # | 文档声称 | 来源 | 验证动作（Phase 1/2） |
|---|----------|------|----------------------|
| S1 | macro-scan v3.8.17 / macro-sim v2.0.40(CACHE14/v2033) / macro-ji v1.0.0 / kaiyang v1.10.8 | STATUS L54 | Phase2: docker inspect 镜像/版本标签 vs 文档 |
| S2 | 容器四枚 + crucix-crucix-1(:3117 Up healthy) | STATUS L14/L77 | Phase2: docker ps 实测 |
| S3 | crucix 退场 14 commit 闭合，观察窗 G0=08-12 / G1=08-15 | STATUS L18/L30 | 今日 08-12 → G0 是否触发？data_fetcher 仍连 :3117? |
| S4 | 天璇 17 Agent(agents.yaml A1-A12+S1-S5)，souls/ 8 个 | STATUS L151-152 | Phase1(A-天璇): grep `id:` 实测；souls 目录计数 |
| S5 | 孤儿 5 项(玉衡死代码/天玑空桩/crucix退场未切/sim_log零产出/kaiyang-wave2副本) | STATUS L124-148 | Phase1 各 agent 复查是否仍存在 |
| S6 | sim_log.db 空目录(P0 未修) | STATUS L98/L142 | Phase2: 容器内科 `sim_log.db` 大小/写入 |
| S7 | news_geo 验收 08-13 06:35 自动化；浏览器复核 17 项待主理人 | STATUS L32 | Phase1(A-开阳)/Phase2: news_geo.json 事件数 |
| S8 | 天玑 trigger 链路 scheduler `tianji_trigger` 每日 09:42 → /workspace/data/tianji_trigger.json | STATUS L78 | Phase2: scheduler JOBS(ast) vs 脚本；cron 时间 |
| S9 | 部署=scp 单文件+sha256，禁 rsync --delete | STATUS L107 | Phase2(B-容器): deploy.sh 实测路径 |
| S10 | R4h① 收编 ease_ok 方向闸；credit 回池/p̂/S2 挂起转 silence | STATUS L73/L109 | Phase1(A-天璇): calib 产物/断言现状 |

## 二、即时结构扫描发现（已是种子，待定性）

| # | 发现 | 证据 | 定性线索 |
|---|------|------|----------|
| S11 | 项目导航.md「文件结构」描述的是工作区布局（世界推演系统天枢/ 等），但 repo 根实际是 macro-scan/ / kaiyang/ / macro-sim/ / macro-ji/ | 项目导航 L17-31 vs `ls /s/world-sim` | 文档-vs-repo 结构不符(A1/C4)，可能 stale 或错置 |
| S12 | 项目导航.md 称「工作区是文档权威」，STATUS.md 称「工作区读写不可信、NAS repo=真相」 | 项目导航 L3-5 vs STATUS L10-12 | 两文档对权威性互相矛盾(A2) |
| S13 | OPEN-DECISIONS.md 有两份副本：macro-sim/docs/decisions/ 与 macro-scan/docs/decisions/，repo 根 docs/decisions 不存在 | Glob 实测 | 双份真相/可能分歧(A4) |
| S14 | SOP.md 不在 repo 根，而在 macro-scan/docs/SOP.md | Glob 实测 | 路径与项目导航引用不一致 |
| S15 | repo 根有 STATUS.md.bak-20260811（15KB，gitignored `*.bak`） | `ls /s/world-sim` | 残留旧文件(C2)，已忽略但留根目录 |
| S16 | repo 根有 kaiyang-wave2/（旧副本，文档称 v1.7.0 落后线上 v1.10.8） | `ls /s/world-sim` + STATUS L147 | 残留旧副本(C1/C2) |
| S17 | repo 根有 fci-recovery/（Aug 10 建，来源/用途不明） | `ls /s/world-sim` | 疑似脚手架残留，待定性(C2) |
| S18 | repo 根有 HANDOVER.md（20KB），MEMORY 注其为「停更降级历史快照」 | `ls /s/world-sim` + MEMORY | 可能 stale 快照(A1) |
| S19 | repo 根无 worldsim-review-synthesis.md / calib-*.md（项目导航称在根），实际在 docs/archive / docs/calib | Glob + ls | 文档结构描述过时(A1) |
| S20 | ADR-*.md 全仓 Glob 无结果，但 STATUS 引用 ADR-08 等 | Glob `**/ADR-*.md` 空 | ADR 文档缺失或命名异(A3) |

## 三、已知前置结论（直接复用，不重复劳动）
- SMB 可靠，早前「不靠谱」= 路径搞错（仓库内死副本 vs 仓库外 `S:\macro-scan\data`）
- 源码树死副本清理已 commit `596780c` push；main upstream 已设
- 12 矛盾点/孤儿 5 项已在 STATUS 登记，本次重裁 resolved/still-open
-嵌套挂载红线(37dda5a 已治本)、soul 路径 P0 仅在 config 放 /tmp 触发
