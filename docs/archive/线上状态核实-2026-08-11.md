# 线上状态核实 + STATUS.md 自相矛盾点裁决（2026-08-11 16:30）

> 核实方法：`ssh nas`（Git 自带 ssh，走 192.168.31.108）+ `docker exec` 直接读运行容器，
> 不依赖 WorkBuddy 工作区代码副本（已证为滞后镜像）。所有结论均带容器内实测证据。

---

## 一、线上容器实测（4 容器全活）

| 容器 | 镜像/版本 | 状态 | 端口 | 备注 |
|------|-----------|------|------|------|
| macro-scan-macro-scan-1（天枢） | image `macro-scan:v7`，代码热挂载 | Up 20 hours | **8899-8900→8899-8900** | 运行代码特征标签 `v3.8.3` |
| macro-sim（天璇） | COPY 重建 | Up 2 days | — | `/app/VERSION = v2.0.40` ✅ 与 STATUS 一致 |
| macro-scan-tianji-1（天玑） | — | Up 23 hours (healthy) | — | 独立验证容器，运转中 |
| macro-scan-kaiyang-1（开阳） | nginx `:8080` | Up 22 hours | 8080 | dist 含 v1.10.x 特性（见下） |
| crucix-crucix-1（信号总线） | — | **Up 5 hours (healthy)** | 内部端口 8000/8080 等，**无主机发布端口** | **仍实时服务** |

---

## 二、版本号实测（与 STATUS 比对）

| 组件 | STATUS 声称 | 运行实测 | 判定 |
|------|-------------|----------|------|
| 天璇 | v2.0.40 | `/app/VERSION`=v2.0.40 | ✅ 一致 |
| 天枢 | **v3.8.17**（NAS 实时 STATUS） | 代码仅特征标签 `v3.8.3`（无单一 version 常量） | ⚠️ 文档标签 vs 代码标签漂移（非功能矛盾） |
| 开阳 | v1.10.8 | dist 含 `报告中心`/`news_geo`/`GSCPI`/`风险信号`（v1.10.x 特性均在） | ✅ 特性与 STATUS 自洽（详见第四节纠正） |
| crucix | "退场中" | 容器健康 + 实时 200，30/30 源 OK | ⚠️ 见第三.3（非矛盾，见下文） |

---

## 三、STATUS 四处"矛盾"逐条裁决（核实能否解决？）

### 1. "仓库 compose 只映射 8899，运行区却是 8899+8900"
**裁决：假矛盾（agent 误读），核实已解决。**
- 实测 NAS 仓库 compose：`ports: "8899-8900:8899-8900"`（两行，8899 与 8900 都在）。
- 运行容器 `docker ps` 端口列也是 `0.0.0.0:8899-8900->8899-8900/tcp`。
- 结论：文档与运行完全吻合，无矛盾。原 agent 报告"只映射 8899"是误读。

### 2. "static_gate_check.py 是稻草人（摆设）"
**裁决：假矛盾（agent 误读），核实已解决。**
- 文件真实存在：`/vol2/1000/software/macro-scan/tests/static_gate_check.py`。
- 内容是对 `fetch_gdelt_geo.py` + `gdelt_country_map.py` 的**实质性 P0 静态闸门**：拦截 `from optim_config import FRED_PROXY`（防 DATA_DIR 落非持久卷）、拦截 `import scan_weak_signals`、拦截单文件 bind mount、拦截硬编码国码。退出码 0/1。
- 结论：是真实单元测试，非稻草人。原 agent"稻草人"判定错误。

### 3. "crucix 退场中 vs 实测活跃 30/30"
**裁决：非矛盾，是同一阶段的两面描述。核实已厘清。**
- 实测：crucix 容器 Up 5h、healthy；macro-scan 的 `CRUCIX_REMOTE_URL=http://192.168.31.108:3117/api/data`；从 macro-scan 容器请求该地址 → **HTTP 200**，返回 `{"meta":{"version":"2.0.0","timestamp":"2026-08-11T08:30:50Z","sourcesQueried":30,"sourcesOk":...}}`。即 crucix **此刻仍在被天枢实时消费**，且 30/30 源正常。
- STATUS 自述："08-10 复核：天枢 6 模块实时消费 crucix API（D1-D6），'过渡期共存'是现状依赖非选择；退场前置=6 项依赖摘除"，观察窗 G0 判 08-12 / G1 判 08-15。
- 结论：**"退场中"=代码已合并（gscpi 替代 + nuke 机制），但运行时切换尚未执行，处于观察窗前**。所以"退场中"与"活跃 30/30"并不冲突——它正在退场、但还没退完，现在仍活着。原 agent 当成矛盾是过度解读。
- **真实 action item（非文档矛盾）**：crucix 运行时确实还没切掉。若要在 08-12 判 G0 后执行退场，需确认 6 依赖摘除脚本已就位并真正切断 macro-scan 对 :3117 的调用。

### 4. "天枢版本号三处不一致（3.8.12 / 3.8.7 / v3.8.11）"
**裁决：该"三处不一致"本身是工作区滞后镜像的产物；实时 STATUS 已自洽。核实已解决。**
- 该三数字来自 **工作区（滞后）STATUS 副本**的不同小节，是那份镜像自身编写时的混乱。
- 实测 **NAS 实时 STATUS.md 第 54 行**：`macro-scan v3.8.17 / macro-sim v2.0.40 / macro-ji v1.0.0 / kaiyang v1.10.8` —— 每个组件**只有一个数字**，自洽。
- 运行代码无单一 `version` 常量，仅 `startup_checks.py` 注释里出现特征标签 `v3.8.3`（"R09 社会情绪压力（v3.8.3 接入）"）。所以"v3.8.17"是文档里程碑标签，"v3.8.3"是代码功能标签——**属文档/代码标签口径不一致，非功能矛盾**。
- **小建议**：STATUS 应注明版本号的口径（如"v3.8.17 = 最近一次功能合并里程碑，代码以功能标签为准"），避免后人误判。

---

## 四、核实纠正了上一轮我自己的误判（重要）

上一轮全量调查时，我曾从运行 dist 里 grep 到 `1.8.0` 便推断"运行开阳 = v1.8.0，低于 STATUS 的 v1.10.8"。**这是错的，已核实纠正：**

- `1.8.0` 出现在 `assets/index-*.js` 三个 chunk 里，是**第三方库版本号**，不是 app 版本。
- 改用**特性标记**反查运行 dist：`报告中心`(3 文件) / `news_geo`(3) / `GSCPI`(3) / `风险信号`(3) 全部命中 —— 这些都是 v1.9.0→v1.10.8 那波加的功能。
- 结论：**运行开阳确实部署了 v1.10.x 特性，STATUS 的 "v1.10.8" 与运行时自洽**。工作区副本的 v1.7.0 才是真的落后（缺这些特性），"工作区=滞后镜像"的判断依然成立。

这正印证了你的担心：单纯 grep 版本字符串会偏移，必须用房特性/运行时证据交叉验证。

---

## 五、剩余真实待办（非文档矛盾，是工程事实）

1. **crucix 运行时未切退**：代码就绪但 :3117 仍在被消费。08-12 G0 判定后若执行退场，需验证 macro-scan 不再依赖 crucix（否则退场会变静默降级）。
2. **天枢版本口径**：STATUS 标 v3.8.17，代码仅 v3.8.3 功能标签，无单一常量。建议文档注明口径。
3. **开阳精确版本未固化**：运行 dist 无干净 `version` 字段，只能靠特性反推 ≥1.9.0。建议在构建产物里固化 `VERSION` 文件（这正是工作区缺、运行时也缺的东西），否则日后无法一眼确认部署版本。
4. **已知 P0 未修项（STATUS 已记，非本次新发现）**：sim_log.db 仿真记录空、时区 OPEN 3 条 —— 维持原状。

---

## 六、一句话结论

**STATUS.md 经线上核实，四处"矛盾"全部可解：3 处是 agent 误读（compose 端口、static_gate 稻草人、crucix 退场表述），1 处是工作区滞后镜像自身混乱（天枢三版本号）；实时 STATUS 与运行容器高度自洽。** 系统没有阻断性文档矛盾，真正的 loose end 是 crucix 运行时退场尚未切执行 + 开阳/天枢版本号缺乏运行时可固化标记。所有诊断结论以 `ssh nas` + `docker exec` 实测为准，工作区代码副本仅作文档参考。
