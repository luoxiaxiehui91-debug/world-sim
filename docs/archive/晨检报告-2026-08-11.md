# 晨检报告 — 世界推演系统 2026-08-11

> 检查时间：2026-08-11 09:45–09:55 (GMT+8)
> 检查方式：`ssh nas` + `docker exec` 实测（Git 自带 ssh），全部证据来自容器内/宿主实测
> 结论速览：**5 项主检全部达标（1 项附注需人工介入）**，无 P0

---

## 总览

| # | 检查项 | 状态 | 摘要 |
|---|--------|------|------|
| 1 | 天枢四首跑 | ✅ 4/4 | gscpi/kiwisdr 实际成功（state last_ok=False 为重启伪影），climate/narrative_proc 成功 |
| 2 | firms 09:08 | ⚠️ | 仍 0 条，无 status=failed，补偿重试未救回 → **需人工介入** |
| 3 | 新 fetcher 产物 | ✅ 3/3 | GSCPI 尾行 0.805 / safecast 6 站 / sdr total=853 |
| 4 | 开阳数据通路 | ✅ | 18/18 feed 全 200；data inode 23462 与宿主一致（挂载健康） |
| 5 | news_export I15 | ✅ | updated=2026-08-11T09:45:22+08:00，40 篇，15 分钟一轮 |

**P0 判定：无。** 无首跑失败（gscpi/kiwisdr 实证成功）、无挂载断链。

---

## 1. 天枢四首跑 — ✅ 4/4

> ⚠ 关键背景：天枢容器 **08-11 06:38:14 重启**（`docker inspect StartedAt=2026-08-10T22:38:14Z`）。
> scheduler.py 的 `last_ok` 为内存字典（P0-D：spawn 成功即 True），重启后清空、不随 last_run_ts 恢复
> → 05:30–06:30 窗口约 18 个固定 job 的 `last_ok=False` **全部为重启伪影，不代表运行失败**（07:10 后新进程 spawn 的 job 均为 True）。

| 首跑 | 计划 | state last_ok | 实证 | 判定 |
|------|------|--------------|------|------|
| gscpi | 05:32 | False（伪影） | 手动重跑 `python3 fetch_gscpi.py` → `[OK] GSCPI 幂等跳过（已有最新 2026-07-31 = 0.805）` EXIT=0；scheduler.log 05:32:07 spawn PID=572 | ✅ **实际成功** |
| kiwisdr | 06:02 | False（伪影） | `/var/log/macro-scan/kiwisdr.log`：`[OK] kiwisdr 写入 853 台（online=853），7/8 区有接收器` | ✅ **实际成功** |
| climate | 09:10 | True | scheduler.log 09:10:20 FIRING+spawn PID=242（G0 第 1 天） | ✅ 成功 |
| narrative_proc | 07:10 | True | 日志 `完成：JSON7条`，**无 `no such column: summary` 报错**（08-01~08-10 每日均有，ADR-08 修复生效） | ✅ 成功 |

## 2. firms 09:08 — ⚠️ 仍 0 条，需人工介入

- `firms_fire.json`（09:08:24 更新）：`total_hotspots: 0`、`active_fire_regions: []`、source=NASA FIRMS VIIRS_SNPP_NRT,VIIRS_NOAA20_NRT (direct)
- **无 `status=failed` 字段**（脚本执行成功，上游返回 0 匹配，非下载失败）
- 结论：补偿重试未救回，**标注需人工介入**（核查 NASA FIRMS 令牌/窗口参数/坐标映射，与 GDELT FIPS 映射坑同族疑点）

## 3. 新 fetcher 产物 — ✅ 3/3

| 产物 | 预期 | 实测 | 判定 |
|------|------|------|------|
| `data/fred_history/GSCPI.csv` | 尾行 2026-07-31,0.805 | 尾行 `2026-07-31,0.805`（文件 08-10 16:32 生成，今晨幂等跳过） | ✅ |
| `data/safecast_nuke.json` | 6 站 | 6 站（Zaporizhzhia/Chernobyl/Bushehr/Yongbyon/Fukushima/Dimona），degraded=false，Chernobyl anom=true | ✅ |
| `data/sdr_summary.json` | total 839±10% | total=**853**（±10% 区间 [755,923] 内） | ✅ |

## 4. 开阳 :8080 数据通路 — ✅

- **18/18 feed 全 200**（含重点 reports_index.json / safecast_nuke.json / fci_latest.json），清单自运行 bundle 提取：
  climate_signals / disaster_signals / earthquake_risk / energy_risk / fci_latest / grv_latest / hdx_risk / fred_history/manifest / market_quotes / news_export / news_geo / news_risk / nuclear_sites / reports_index / safecast_nuke / sim_trigger / spacetrack + manifest
- **挂载健康**：容器 `/usr/share/nginx/data` inode = **23462** = 宿主 `/vol2/1000/software/macro-scan/data` inode 23462 ✅
- nginx 配置符合 08-11 嵌套挂载治本修复：`location /data/ → alias /usr/share/nginx/data/`（独立挂载 + `try_files $uri =404`），html/data 嵌套已消除
- 容器内 curl localhost:8080 失败为端口误测（nginx listen 80，宿主映射 8080），非故障

## 5. news_export I15 — ✅

- `news_export.json`：`exported_at`/`updated` = **2026-08-11T09:45:22+08:00**（带显式 +08:00 后缀，符合时区契约），40 篇
- news_export.log：09:30:21 与 09:45:21 连续两轮 export 成功，I15 提频生效

---

## 附注与建议

1. **容器 06:38 重启原因待确认**（部署 or 手动）。影响：05:30–06:30 窗口 job 的 last_ok 语义失真，若监控以 last_ok 判定会误报。建议：确认重启原因并记录到 STATUS.md；可选优化——scheduler 启动时从 state 恢复 `_last_run_ok` 或在 state 中记录进程启动时间戳以便区分伪影。
2. **firms 需人工介入**（P1，非 P0）：连续 0 条，补偿重试未救回。核查方向：NASA FIRMS API token 有效性、upstream_window_days=2 参数、坐标/区域匹配逻辑。
3. 无 P0。climate（G0 第 1 天）与 narrative_proc（ADR-08 后首个无报错运行）均正常，属今日正向里程碑。
