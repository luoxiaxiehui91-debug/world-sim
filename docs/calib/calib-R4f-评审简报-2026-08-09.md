# calib-R4f 评审简报（输入材料，v1）

> 日期：2026-08-09 01:18
> 评审对象：天璇 macro-sim 校准引擎 A2 商业银行行为（EPS 校准，一致率评分体系）
> 评审议题：R4f 修复方案三选一/组合（①direction EASE tightening 0.5→0.65 ②activation 0.70→0.85 ③grv_down A3/A10 独立处理）
> 上游评审资产：R4a→R4e 五轮迭代 + 三方（arch/qa/data）评审结论均已落档（见 §5 参考）

---

## 0. 背景（自包含）

校准引擎对 3 个内生变量做 LLM 调参拟合：market_sentiment（w=0.40）/ bank_credit_tightening（w=0.35）/ liquidity_premium（w=0.25）。评分 = 加权一致率（T 类步 d·t≥0 占比），验收协议 = 5 seed（42/7/123/2024/777）合并 + fail-fast 判定（dead/silence 硬闸 → 合并 CI 下限≥0.55 → 点估≥0.60 → per-seed≥0.50 → 回退闸）。**接受线：合并 p̂≥0.616（N≈230 下 CI 下限≥0.55 等效）；partial 线 p̂≥0.55。weighted 0.60 门槛冻结禁调。**

## 1. R4a→R4e 迭代成果（已部署，commit 均 push）

| 轮次 | 改动 | 结果 |
|------|------|------|
| R4a | grv 触发线契约回退 0.8 + S 类 why-no-action 归因 + 验收脚本基建 | 归因定位：credit S 类 rate_limit=0.815（info_delay 冷却主因） |
| R4b | info_delay 2→1 | act 0.265→0.388 ✓ 但暴露方向冲突（credit consistency 0.769 假象→0.476 真值） |
| R4c-A | dead 判定语义修正（m_v_active 口径） | credit 入池，p̂=0.477 |
| R4c-B | A2 方向对齐设计（方向闸+方向 EASE+EPS 中性带+cs_delta 经 world 属性）三方会签 | 设计通过，危机豁免 vix>0.5 实测 62% 超线→终裁删除/收窄 vix>1.0 |
| R4d | 方向闸+方向 EASE（spread 250→350/grv 0.25→0.4）+ vix>1.0 回退预案 | **credit consistency 0.476→0.643 达标**（方向对齐兑现）；silence 回退 0.429→0.51（EASE 触发率低 hold 62-71%） |
| R4e | grv 0.4→0.6 + ease-block 三分类归因 | **credit 入池（silence median 0.49≤0.50）p̂ 0.494→0.515**；consistency 0.643→0.562（合并 0.584，回退不显著 p=0.55） |

## 2. R4e 终态（当前引擎实测，5 seed）

| seed | n_active | m_v_active | act | silence | consistency |
|------|----------|------------|-----|---------|-------------|
| 42 | 16 | 0.335 | 0.33 | 0.49 | 0.562 |
| 7 | 14 | 0.335 | 0.29 | 0.53 | 0.714 |
| 123 | 14 | 0.335 | 0.29 | 0.53 | 0.500 |
| 2024 | 17 | 0.335 | 0.35 | 0.47 | 0.471 |
| 777 | 16 | 0.335 | 0.33 | 0.49 | 0.688 |
| median | 16 | 0.335 | 0.33 | 0.49 | 0.562 |

- merged p̂=0.515 | CI 下限=0.439 | N=161.7 | eligible=[sent,credit,lp]（credit 已入池）
- per-seed weighted：42=0.456 / 7=0.534 / 123=0.481 / 2024=0.540 / 777=0.478
- EASE ship 闸 5 seed 全 PASS；回退线无 revert（credit_silence met，其余 warn）

## 3. ease-block 归因（R4e 新增测量，下一杠杆定位）

| seed | grv_ge_06 | spread | tightening_ge_05 | would_fire | tighten_fail |
|------|-----------|--------|------------------|-----------|--------------|
| 42 | 0.30 | 0.0 | 0.15 | 0.35 | 0.20 |
| 7 | 0.21 | 0.0 | **0.42** | 0.21 | 0.16 |
| 123 | 0.38 | 0.0 | 0.19 | 0.24 | 0.19 |
| 2024 | 0.35 | 0.0 | 0.05 | 0.35 | 0.25 |
| 777 | 0.65 | 0.0 | 0.0 | 0.29 | 0.06 |

- **grv_ge_06 仍最大挡（0.21-0.65）——grv 杠杆已用尽（再放宽伤 consistency 且边际递减，data-r2 已确认）**
- **seed7 特异性：tightening_ge_05=0.42（tightening≥0.5 挡死 42% EASE 步）**
- spread 永不挡；would_fire 0.21-0.35（无 ease 条件挡但仍 HOLD=冷却/激活路径）；tighten_fail 0.06-0.25

## 4. R4f 候选方案（来自 arch-r2 R4e 建议 + data-r2 统计复核）

| 方案 | 改动 | data-r2 统计优先级 | 预期 |
|------|------|-------------------|------|
| ① | direction EASE 的 tightening 限制 0.5→0.65（financial.py） | **① 最高**——seed7 特异性解绑，局部修改风险集中 1 seed，直接提 n_active 与 silence 余量；EASE 后 tightening 仍高=银行有空间，风控语义可辩 | credit 最后拼图 |
| ② | activation 0.70→0.85 | **不建议**——全局参数影响所有 agent（含 sent/lp），风险面最大；R4b 已实证增益有限（act 0.265→0.388 后活性已达标）；压 would_fire 收益不确定 | 不推荐 |
| ③ | grv_down 0.273 独立处理（A3/A10 写者，非 A2 范围） | 次高——sent 权重最大（0.40），grv_down 0.273→0.40 的 p̂ 提升空间最大 = **p̂ 提升主杠杆**；但属 sentiment 恢复结构，需单独设计 | p̂ 提升主杠杆 |

data-r2 综合裁决：**① > ③ > ②**；①+③ 组合预期 p̂ 0.53-0.58（仍难达过闸点估 0.62，大概率 partial）。

## 5. 本评审任务（R4f 三方会签）

请三方（arch/qa/data）独立评审：
1. **①方案（tightening 0.5→0.65）**：是否值得实施（seed7 特异性解绑 vs 全局参数放宽的语义风险）；对 EASE ship 闸（ease_signal 可达性）/flip-flop（2 步冷却兜底）/consistency 的连带影响；与 R4e 的 grv 放宽（0.4→0.6）是否同族（是否会重复"放宽换活性损质量"模式）
2. **③方案（grv_down A3/A10）**：是否本轮做（sentiment 恢复结构的改动面评估）还是单独一轮；A3/A10 写者调参的方向与回退
3. **②方案**：确认否决或给补充理由
4. **R4f 验收口径**：沿用 5 seed fail-fast 协议？partial 线 p̂≥0.55 是否维持？若 ①+③ 后 p̂ 落 0.53-0.58 的处置（接受 partial 记录残留 vs 继续 R4g）
5. **CACHE_VERSION**：11（当前 10）bump 确认
6. 产出：R4f 实施规格（改动点+预期+回退+验收）

## 6. 红线（不变）

- weighted 0.60 门槛冻结，禁调（调门槛=自证）
- EPS_TGT=0.03 冻结；tests 断言不降（当前 guards 82 + narrative 12 = 94）
- 验收证据只用新探针多 seed median，禁 calibration_cache 自证
- 部署通道：docker build + compose up --force-recreate + md5 双端校验（docker-compose.yml 无 build 段，必须手动 docker build）

## 7. 参考文档

- C:\Users\luoxi\WorkBuddy\世界推演系统\calib-eps-act-review-R1-综合评审-2026-08-08.md（R1 终局 + audit 复核）
- C:\Users\luoxi\WorkBuddy\世界推演系统\calib-c1c3-review-终局裁决-2026-08-07.md（C1-1a/C3-3a 终局）
- macro-sim/docs/r4c-b-a2-direction-alignment.md（方向对齐设计）
- 代码：/s/world-sim/macro-sim/core/（calibrator.py / financial.py / simulation.py / world_state.py / config/agents.yaml / scripts/run_probe_acceptance.py）
