# calib-R4h 评审简报（输入材料 v1）

> 日期：2026-08-09 17:40
> 评审对象：天璇 macro-sim 校准引擎 A2 商业银行（EPS 校准，一致率评分体系）
> 评审议题：R4h ③ sentiment 写者结构实施（本轮）+ ② vix 豁免治理（后行）
> 上游：R4g 终局（归因修正 + 冷却修复证伪回滚 v2.0.37）；R4f 终局（三案否决）；R4a→R4e（五轮迭代）
> 状态：设计材料已齐（arch ③ spec / qa 验收口径 / data 基线快照），讨论轮 1/3 已收 2 票（qa+arch 均推荐 A 参数 + 分步），待完整流程（补 data 票 → 实施 → 验收）

---

## 0. 背景（自包含）

校准引擎对 3 个内生变量做 LLM 调参拟合：market_sentiment（w=0.40）/ bank_credit_tightening（w=0.35）/ liquidity_premium（w=0.25）。评分=加权一致率（T 类步 d·t≥0 占比）。验收=5 seed（42/7/123/2024/777）fail-fast：闸①逐 seed silence≤0.50 → 闸②CI 下限≥0.55 → 闸③点估≥0.60 → 闸④per-seed weighted≥0.50 → 闸⑤回退线 5 条。**接受线 p̂≥0.616；partial 0.55；weighted 0.60 冻结禁调；EPS_TGT=0.03 冻结。**

## 1. R4a→R4g 七轮迭代结论（锚）

| 轮次 | 改动 | 结果 |
|------|------|------|
| R4a | grv 触发线回退 0.8 + S 类归因 + 验收基建 | 归因：credit S 类 rate_limit=0.815 |
| R4b | info_delay 2→1 | act 0.265→0.388 ✓ 但暴露方向冲突（0.769 假象→0.476） |
| R4c | dead 判定 m_v_active + A2 方向对齐设计 | credit 入池 p̂=0.477 |
| R4d | 方向闸+方向 EASE+vix>1.0 豁免 | credit consistency 0.643 达标；silence 回退 0.51 |
| R4e | grv 0.4→0.6 + ease-block 归因 | credit 入池 p̂=0.515；consistency 0.643→0.562 |
| R4f | 三案否决（①+0.001 伪影 ②activation 无效 ③结构性不可达） | 零改动，转 R4g |
| R4g | 归因修正 + 冷却修复实施证伪 + revert | **归因映射修复保留（tsf 可测），引擎回 v2.0.37 = R4e 行为** |

**R4g 三大归因伪影实锤**：①rate_limit 高估 +0.264（S 类子集口径 vs 完整 steps）；②rule_hold 字段不存在（幻影）；③tighten_signal_false 死代码（a2_acted 对 HOLD 判 True）。修正后真实归因=激活机制 92%（冷却 61.8%+概率 30.1%），规则层 8.1%。

## 2. 当前基线（v2.0.37 = R4e 行为 + 测量修复，data-r4h 容器实测）

**credit 四指标（逐 seed）**：silence 42:0.490/7:0.531/123:0.531/2024:0.469/777:0.490（median 0.490）；n_active 16/14/14/17/16（median 16）；act 0.327/0.286/0.286/0.347/0.327；consistency 0.562/0.714/0.500/0.471/0.688（median 0.562）。**merged p̂=0.5152，Wilson CI 下限 0.4387**。

**sentiment 基线（③ 对比基准）**：
- grv_down reverse median **0.727**（42:0.727/7:0.591/123:0.762/2024:0.619/777:0.727）；全 T 0.488；grv_up 0.227
- A2 写 sentiment：TIGHTEN -0.08 ×12 步/seed vs EASE 不写 ×4 步 → 负写者主导 3:1
- sentiment level_mean -0.880 / floor_frac 0.694 / clamp_frac 0.388-0.653（恒贴 floor）

**vix 基线（② 对比基准）**：TIGHTEN wrong=**17 步**（42:4/7:3/123:4/2024:5/777:1），**100% 豁免**（vix_stress>1.0）；vix_stress>1.0 步 23-38/seed；vix 峰值 162-238（**无 decay，存量不回吐**）。EASE wrong=**9 步**（A2 决策级，c/w/n=15/9/0，rate 0.625）。

**归因分布（修正映射后，credit S 类）**：rate_limit 0.417-0.692 / activation_gate 0.192-0.458 / tighten_signal_false 0.0-0.130（seed7 0.038/123 0.115/2024 0.13/777 0.125）/ acted_other 0。

## 3. R4h ③ 实施 spec（arch-r4h 定稿，容器验证行号）

**改动**：simulation.py:147 后插入一行 `add("A2", "market_sentiment", +K * m)`（K=参数；m=mag("A2")=1.0）。TIGHTEN 写 sentiment 在 L141（-0.08×m），EASE 分支 L142-147 现不写 sentiment。
- clamp [-1,1]（apply_sentiment_delta 统一）；damping 恒 1（A=1.0）
- **传导放大**：A2→A3(to_A3=0.40)/A10(to_A10=0.35)，衰减 0.5 → 每步总效果 K×0.34375（K=1.0 → +0.0275/步 ≥EPS_ACT）
- S→T 转移：EASE 步 sentiment d 从 0 转 +0.0275 → sentiment n_active 升、eligible 池变 → merged p̂ 结构变化

**③→② 传导路径**：EASE 写 sentiment(+K) → sentiment 抬离 floor → consecutive_negative_steps 重置 → vix bleed 停（world_state.py:277-280 需 sentiment<-0.5 ∧ 连续3步）→ vix 不再新涨。**但 vix 无 decay 存量不回吐** → ② 部分收敛，③ 必要不充分。

**断言影响**：既有 110 无 FAIL；新增 3 项（test_a2_ease_writes_sentiment / test_a2_ease_sentiment_symmetry / test_a2_ease_sentiment_t_class）→ 113。CACHE_VERSION 11→12；VERSION v2.0.38；ARTIFACT_TAG v2031。

**a2_action 落盘（qa 前置依赖）**：calibrator.py:725-739 step_record 加 `"a2_action": snapshot.get("actions",{}).get("A2","HOLD")`（纯测量层，无额外 CACHE bump，服务 P1/M4/M1）。

## 4. 参数候选（已 2/3 票推荐 A）

| 候选 | 机制 | 预期 grv_down reverse | 判定 |
|------|------|----------------------|------|
| **A** +0.08（K=1.0） | 与 TIGHTEN 完全镜像 | 0.52-0.62（达标 ≤0.60） | **qa+arch 均推荐** |
| B k=0.75 | 可调保险 | 0.56-0.66（可能 warn） | qa：保险冗余（过冲已被矩阵捕获）；arch：保险针对不存在的 A1 HIKE 风险 |
| C +0.04 | 保守 | 0.60-0.68 | **与 B(k=0.5) 传导等价，排除** |

**arch 量化**：A 过冲恐惧不成立——EASE 正贡献仅占窗口负压总量（-3.3~-6.5）的 3-5%，A1 HIKE>0.5 概率≈0（安全边际 ~5 倍）；真正行为变化=A3 -0.4 边界复激活（floor_frac 0.694 → 70% 步 A3 被动 HOLD；抬过 -0.4 后 A3 重新响应，方向正确）且被三向自限。

**批次裁决（2/3 票推荐分步）**：③ 先行、② 后行。同批让 M2/M6/S2 三个硬闸归因全部混淆；② 豁免前提方案（vix_stress>1.0 ∧ sentiment>-0.3）结构上依赖 ③（无 ③ 时 sentiment 恒<-0.3 → 前提恒不满足 → 豁免恒关 → R4d 沉默回归重演）。M6 天然成为 ② 裁决闸：③ 后 TIGHTEN wrong 残差 ≤17 → ② 可轻量或不做。

## 5. 验收口径（qa-r4g 定稿，docs/r4h-acceptance-criteria.md）

- **主闸序①-⑤ + 回退线 5 条 + partial 0.55 / weighted 0.60 / EPS_TGT 0.03 冻结**（不变）
- 前置：P1 a2_acted 检查 / P2 归因双报 / P3 反作弊门 / P4 ARTIFACT_TAG=v2031
- **硬闸**：M2 silence diff>+0.05｜M4 flip==0（EASE 后 2 步内 TIGHTEN 决策）｜M5 credit consistency<0.45｜**M6 TIGHTEN wrong>17**｜S2 grv_down reverse≥0.727
- **观察**：M1 EASE wrong（基线 9，A2 决策级同口径）｜M3 rate_limit 双口径｜S1 sentiment 桶
- **③ 单独轮必看 3 项**：①M6 残差（裁决 ② 必要性）②M2 silence diff（防豁免关闭重演 R4d 沉默）③S1 sentiment 桶（抬离 floor 但无 ≥0.5 尖峰）
- **M7 观察项（qa 建议）**：A1/A3 action 分布前后对比 + vix 存量轨迹（step_record 已落 a1/a3/vix）

## 6. 本评审任务（R4h 完整流程）

1. **data 票**（补）：A vs B 的 merged p̂ 影响估算（sentiment consistency +0.10-0.15 → p̂ 0.5152 → ?）；A 过冲触发概率（用 sentiment 分布数据验证/反驳 arch 的"A1 HIKE≈0"）；③② 分步时"③ 后 vix_stress 降到多少算收敛"机读判据
2. **实施**：③-A（K=1.0）+ a2_action 落盘 + CACHE 12 + VERSION v2.0.38 + ARTIFACT_TAG v2031 + 断言 113 + 规格变更登记
3. **验收**：qa 独立跑（五闸 + M1-M7 + ③ 专项 3 项）；data 前后对比（sentiment 桶/vix/归因/EASE wrong）
4. **② 裁决**（③ 后）：M6 残差 ≤17 → ② 轻量或不做；残差高 → ② 候选三选一（vix decay / 豁免非连续前提 / bleed 上限）单独裁决

## 7. 红线（不变）

- weighted 0.60 冻结禁调；EPS_TGT=0.03 冻结；断言数不降（113 起）禁 skip/.only
- 验收只读落盘工件、禁 calibration_cache 自证；--read-only
- 部署：docker build + compose up --force-recreate + md5 双端（docker-compose.yml 无 build 段，手动 docker build）
- CACHE_VERSION 每轮独立 bump（③→12、②→13）

## 8. 参考文档

- C:\Users\luoxi\WorkBuddy\世界推演系统\calib-R4g-review-终局裁决-2026-08-09.md（R4g 终局）
- C:\Users\luoxi\WorkBuddy\世界推演系统\calib-R4f-review-终局裁决-2026-08-09.md（R4f 终局）
- C:\Users\luoxi\WorkBuddy\世界推演系统\calib-R4f-评审简报-2026-08-09.md（R4f 简报）
- macro-sim/docs/r4h-design-draft.md（R4h 设计初稿，arch）
- macro-sim/docs/r4h-acceptance-criteria.md（R4h 验收口径，qa）
- C:\tmp\r4h_data\R4H_基线快照.md（基线快照，data）
- 代码：/s/world-sim/macro-sim/core/（calibrator.py / financial.py / simulation.py / world_state.py / config/agents.yaml / scripts/run_probe_acceptance.py）
