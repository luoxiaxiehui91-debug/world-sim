# 生成文件/运行时文件
**/__pycache__/
**/*.pyc
**/*.pyo
**/.DS_Store
**/Thumbs.db

# 数据目录（各子项目运行时产生，不纳入版本控制）
data/
macro-scan/data/
macro-scan/logs/
macro-scan/docs/分析报告/
macro-scan/docs/新闻库/
macro-scan/docs/macro_dashboard.html

# 知识库（研究内容，不随仓库分发）。
# 本仓库开源的是框架与逻辑；知识库由使用者自建：
#   python3 scripts/init_kb.py      生成示例骨架
#   docs/KB_SETUP.md                格式与目录约定
# 如需放到别处，用环境变量 KB_ROOT 指定。
macro-scan/知识库/

# 敏感配置（各子项目保留自己的 .gitignore 处理 key.txt 等）
macro-scan/key.txt
macro-scan/docker-compose.yml

# macro-sim 运行时
macro-sim/output/
macro-sim/sim_log.db

# 备份/临时文件
backups/
*.bak
kaiyang.bak*/
macro-scan/核心代码/NDH6SA~M
macro-scan/核心代码/qa_result.txt
.hermes_task.md

# 08-18 P0-1：天玑 compose 的 .env（WORLDSIM_APP_PW，NAS 本地，禁入 git）
macro-ji/.env
**/.env
*.bak-*

# pytest 覆盖率产物（macro-sim/.coverage 等）
**/.coverage
