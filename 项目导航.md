# world-sim 项目文档

> 同步镜像（2026-08-10）：本文档与 STATUS.md 同步自 WorkBuddy 工作区，repo 侧镜像位于仓库根。权威实时版以工作区为准；repo 侧随治理轮次同步。

项目根：`C:\Users\luoxi\WorkBuddy\世界推演系统\`（WorkBuddy 工作区；NAS repo 根 = `/vol2/1000/software/world-sim/`）

## 项目状态（2026-08-10）

- Sprint-0 止血线（08-01）完成；开阳实时化 + 时间审计（08-05）闭环；治理四方向（08-06）定稿
- **当前主线 = 天璇校准引擎 R4 系列治理（08-07→08-10，v2.0.37→v2.0.40）**：R4h ① 于 08-10 结案——收编 ease_ok 方向闸（EASE wrong 8→0），credit 回池/p̂/S2 挂起转 silence 治理。详见 `STATUS.md` R4 系列章节 + `calib-R4h-评审简报-2026-08-09.md` + `macro-sim/docs/r4g-spec-change-registry.md`
- **最近重大教训（P0）**：load_agents 的 soul 加载路径依赖 config 所在目录（`dirname(config_path)/../souls`）——探针 config 放 /tmp 会静默丢失 A3 soul（回退旧决策），方案预期 0.5729 系假复现。一切探针/实验 config 必须放容器真实目录并验证 soul 完整加载

## 文件结构

```
├── 项目导航.md
├── STATUS.md
├── worldsim-review-synthesis.md
├── 世界推演系统天枢/             # 天枢（macro-scan）全部内容
│   ├── SOP.md
│   ├── 采集频率矩阵.md
│   ├── compute_probit.py
│   ├── ged_analysis.py / ged_codebook_extract.py
│   ├── design/                 # 设计文档 + NAS 代码副本
│   ├── data/                   # probit / fred 本地数据
│   └── _archive/               # 天枢调研归档
├── 世界推演系统开阳/
│   └── docs/A3a-控制API-开阳对接文档.md
└── 世界推演系统天璇/
└── .workbuddy/
    ├── research/              # 2026-08-04 全量只读调研产出（见 research/README-调研文档索引.md）
    │   ├── 调查报告-世界推演系统全景调研终版.md   # 唯一权威主报告（§0–§12）
    │   ├── working-papers/    # 总汇编 + T0~T6 各层原始底稿 + 盲区补齐
    │   ├── sources/           # NAS 源文档（SOP/HANDOVER/ROADMAP/arch_review）本地副本
    │   └── evidence/          # _compute_fci_disasm.txt（FCI 反编译）+ 探测脚本
    └── memory/                # MEMORY.md（长期硬事实）+ 2026-MM-DD.md（每日流水）
```

> **调研与知识库入口**：本轮（2026-08-04）全量只读调研的所有文档已归入 `.workbuddy/research/`（索引见 `README-调研文档索引.md`）；发现的 5 个异常已登记到 NAS 中央知识库 `/vol2/1000/software/docs/questions/world-deduction/`（根 `INDEX.md`「世界推演」section）。历史调查归档见 `世界推演系统天枢/_archive/`。

## 待办

1. **R4h ① 挂起项 → silence 治理立项**（credit 回池 / p̂ 过 0.55 / S2≤0.60；seed123 残余弱项）
2. **news_geo 图层空渲染**（P3-A 架构工作：拆文件/补 NER）
3. FRED 上游源停更观察（BAA10Y/DTWEXBGS 根因待查）
4. 天璇 sim_log.db 空目录（仿真记录功能损坏，P0 未修）
5. A3a 控制 API 后续增强（HTTP REST :8900 已落地，增强项按需规划）

## 部署

- 容器 `macro-scan-macro-scan-1`，NAS `nas`，Git 仓库 `/vol2/1000/software/world-sim/` → GitHub `luoxiaxiehui91-debug/world-sim`
- 代码热挂载 `/vol2/1000/software/macro-scan/核心代码/` → 容器 `/app/`
- 数据 `/workspace/data/`
- 开发从 Git repo 出发，通过 scp 单文件 + sha256 校验部署
- scheduler.py 改动需 `docker restart`，其余 .py 热挂载
