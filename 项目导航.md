# world-sim 项目导航

> 同步镜像（2026-08-11）：本文档与 STATUS.md 同步自 WorkBuddy 工作区，repo 侧镜像位于仓库根。权威实时版以工作区为准；**新 session 冷启动先读 `STATUS.md`（含"新 session 冷启动"区块：项目位置/SSH/容器/主线/红线）**，本文档为结构概览。

项目根：`C:\Users\luoxi\WorkBuddy\世界推演系统\`（WorkBuddy 工作区——文档权威：STATUS/项目导航/calib 在此）；**NAS repo 根 = `/vol2/1000/software/world-sim/`（真相，SSH 用 Git 自带 ssh `ssh nas`）**；`S:\world-sim` = 同一 repo 的 SMB 挂载视角（`\\192.168.31.108\software\world-sim`，与 NAS 路径同一份文件），仅浏览参考、读写不可信（判脏/部署走 ssh）

## 项目状态（2026-08-11）

- **主线 1 = crucix 退场（08-10 论证 → 08-11 实施闭合）**：14 commit 全闭合；观察窗中——G0 判 08-12 / G1 判 08-15（gscpi 双轨 5 天），之后 WP-2.1b gscpi 切换 / WP-2.2 nuke 改读 / WP-3.x 清理 / WP-4.x 停容器。论证文档：`macro-scan/docs/arg-round1-*.2026-08-10.md` + `arg-map-*`（08-11 归档入 repo）
- **主线 2 = 开阳补全（v1.9.0→v1.10.8）**：报告中心（45 份）/ FCI+GSCPI / 风险信号面板 / news_geo 事件图层（137B→527 事件）/ 视觉 crucix 化 / 同地点聚合（628→208 点）。news_geo 验收观察窗 08-13 06:35 自动化判定；浏览器复核 17 项待主理人
- **R4 系列（天璇校准，08-07→08-10）已结案**：R4h ① 收编 ease_ok 方向闸（v2.0.40）；credit 回池/p̂/S2 挂起转 silence 治理
- **时区统一修复（08-11）**：全系统落盘时间戳显式后缀（UTC→Z / 本地→+08:00），前端 parseTs 契约「无后缀=北京时间」
- **最近重大教训（P0）**：① load_agents soul 加载路径依赖 config 目录——探针 config 放 /tmp 静默丢 A3 soul（假复现）；② docker 嵌套 bind mount 高危（mv 父挂载源丢子挂载）——kaiyang 已治本 37dda5a（data 独立挂载 + nginx alias）

## 文件结构

```
├── STATUS.md                  # ★ 交接文档（新 session 冷启动第一入口）
├── 项目导航.md                # 本文件（结构/待办概览）
├── ROADMAP.md                 # 工作区摘要版（R4/crucix/开阳状态；repo 权威完整版在仓库根）
├── AGENTS.md                  # 工作区精简版（阅读路径/操作约束；repo 权威版在仓库根）
├── README.md                  # 工作区入口
├── worldsim-review-synthesis.md  # 架构设计权威（仅需查架构决策时读）
├── calib-*.md                 # R4 评审/裁决权威（工作区）；repo 镜像 docs/calib/
├── 世界推演系统天枢/          # 天枢（macro-scan）内容：SOP.md / compute_probit.py / design/ / data/ / _archive/
├── 世界推演系统开阳/          # 开阳 docs（控制 API 对接等 133 文件）
├── 世界推演系统天璇/          # 天璇（macro-sim）内容
├── _tianji_docs/              # 天玑设计/backlog/部署文档（tianji-design.md 等）
└── .workbuddy/
    ├── memory/                # MEMORY.md（长期硬事实+红线）+ YYYY-MM-DD.md（每日流水，append-only）
    └── research/              # 2026-08-04 全量调研产出（索引见 research/README-调研文档索引.md）
```

## 部署速查

- 容器四枚：`macro-scan-macro-scan-1`（天枢，热挂载，:8899 WebUI/:8900 Control API）/ `macro-sim`（天璇，COPY）/ `macro-scan-tianji-1`（天玑，healthy）/ `macro-scan-kaiyang-1`（开阳，nginx :8080）
- 天枢代码热挂载 `/vol2/1000/software/macro-scan/核心代码/` → 容器 `/app/`；数据 `/workspace/data/`；**scheduler.py 改完必须 docker restart**
- 开阳部署：本地构建（禁 SMB）→ scp 原地覆盖 dist（**禁 mv 换 inode**）→ chmod -R a+rX；data 挂载独立路径（`/usr/share/nginx/data` + nginx alias，勿改回嵌套）
- 天璇 COPY 模式：改码须 deploy.sh 重建镜像
- 开发从 Git repo 出发，scp 单文件 + sha256 校验部署；git push 走代理 `http://192.168.31.108:7890`（代理会抖，失败重试）

## 待办（详见 STATUS.md「待做/已知遗留」9 条）

1. silence 治理立项（credit 回池/p̂ 0.4894/S2 0.636，R4h ① 挂起）
2. news_geo 浏览器复核 17 项（主理人）
3. FRED 上游停更根因（BAA10Y/DTWEXBGS）
4. 天璇 sim_log.db 仿真记录修复（P0 未修）
5. 时区 OPEN 3 条（news.db 展示层 / grv-history 边界 / 纯日期键）
6. **航班走廊线（air 图层 B 完整版）待拍板**
